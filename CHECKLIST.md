# EdenExplorer — Development Checklist

Phase-by-phase delivery tracking. Companion to `PRD.md` (the *why* and detail
behind each item) and `CLAUDE.md` (engineering conventions).

**States:** `Completed` · `In-Progress` · `Pending`

> A phase is only marked `Completed` after it has been verified working in the
> running app — not merely compiled. Update this file immediately after that
> verification, per the Operational Instructions in `CLAUDE.md`.

---

## Phase 1 — Core file browsing

| State | Item |
|---|---|
| Completed | Drive-root ("This PC") startup view with type, capacity, and usage per drive |
| Completed | "This PC" sorted by drive letter, independent of leftover folder sort keys |
| Completed | Asynchronous / streaming directory scanning (non-blocking UI) |
| Completed | NT-level filesystem access for enumeration |
| Completed | Back / Forward / Up navigation |
| Completed | Full navigation history saved and restored across sessions |
| Completed | Breadcrumb address bar with inline editing |
| Completed | Windows environment-variable `PATH` expansion in the address bar |
| Completed | **`shell:` URI / CLSID address-bar resolution** — typing a `shell:` moniker (e.g. `shell:ControlPanelFolder`, `shell:Desktop`) or a raw `::{GUID}` path into the address bar resolves it through the real Win32 shell namespace (`SHCreateItemFromParsingName`, the same call already used elsewhere for ordinary paths). If it resolves to a real filesystem location (`shell:Desktop`, `shell:Downloads`, etc.), navigation continues in-app exactly like any other path; if it's a genuinely virtual shell folder this app has no rendering model for (Control Panel, Printers, ...), it's handed off to the shell itself via `ShellExecuteW` so it opens in its own native window instead. The two already-special-cased monikers (`shell:MyComputerFolder`, `shell:RecycleBinFolder`) keep resolving to this app's own in-app sentinel views, unchanged. |
| Completed | Show / hide hidden objects toggle |
| Completed | Type-to-filter in the current directory with fuzzy matching. **Follow-up (visual polish):** the inline query box that pops up when typing (`handle_global_actions`, `itemviewer_helper.rs`) was a completely bare `egui::TextEdit` with no surrounding frame at all - no border, no background, no padding on any side. Wrapped it in the same themed rounded-box pattern already used for the navbar's own search query field (`palette.input_field_bg` fill, `borders_default` stroke, `palette.small_radius` corners, real left/top/bottom padding) so it reads as a real input field instead of a stray line of unstyled text floating over the file list. |
| Completed | Cached filter indices for performance |
| Completed | Status bar below every view (item count, files, folders, total size, size-aware selection summary) — present in every view including tag views and independently in both panes of a split |
| Completed | **Global search, powered by voidtools Everything or a built-in filesystem walk** — a search icon (and Ctrl+F) in the navigation bar of every pane opens an inline query box with a "This folder" / "Everywhere" scope toggle ("This folder" is recursive - it searches all subfolders); submitting opens a normal tab (its own `::SEARCH::`-sentinel virtual location, same trick as This PC/Recycle Bin/tag views) populated with real `FileItem`s - since it's populated the same way a real directory listing is, every existing display mode (Details/Gallery/Columns/ColumnPreview/Preview/DetailPreview) and split-pane already work with no new rendering code. A "Search engine" setting (Settings → Behavior, alongside the default-scope setting) picks Everything (near-instant, index-backed) or Built-in (always available, walks the real filesystem live); if Everything isn't installed/running, Built-in is used automatically regardless of the setting. Search results show an extra "Original Directory" column (reusing the Recycle Bin's own column, since both need "where did this actually come from") since matches can come from many different folders, unlike a normal listing. Result count capped at 5,000 per query for either engine. Supports `key:value` filter syntax alongside plain text - `ext:pdf`, `size:>10mb`/`size:<1kb`, `modified:today`/`modified:yesterday`/`modified:thisweek` - which Everything's own query parser already accepted natively; the built-in engine gained a matching `parse_search_filters` parser (`core/fs.rs`) so the same query works identically on both engines. An info icon next to the search box surfaces the exact filter syntax so it's discoverable without reading docs. |
| Completed | **Saved Searches** — a bookmark icon next to the search box saves the current query + scope (name defaults to the query text, renamable afterward) into a new dedicated "Saved Searches" sidebar section (its own collapsible group, mirroring Tags' structure - not folded into Favorites, since a saved query/scope isn't "a folder visited often"). Clicking a saved search reopens it as a normal search tab via the same `open_or_focus_search_tab` a live search uses; right-click gives Open in new tab / Rename / Delete. Persisted to its own `saved_searches.bin` (mirroring Tags' persistence pattern), capped at 50 entries (a one-click save accumulates faster than deliberately dragging a folder into Favorites, so an unbounded list would clutter the sidebar) - the save button shows a "limit reached" message instead of silently failing once at the cap. |
| Completed | **Content search** — `content:<word>` filter, in addition to `ext:`/`size:`/`modified:`. For the built-in engine, matches are found by streaming a case-insensitive line scan (`core/fs.rs`'s `matches_content_filter`) over candidate files that already pass every other filter, skipping directories, anything over 8MB, and anything that doesn't look like text (reusing `core::preview`'s existing `is_known_text_extension`/`sniff_is_text` checks) - deliberately restricted to a specific folder scope (not "Everywhere"), since scanning file contents across every drive would be far slower than a normal filename search. For the Everything engine, `content:` is Everything's own native query keyword and needs no code change - but it only returns matches if the user has separately enabled Content Indexing in Everything's own settings (off by default, with no way for this app to detect that via IPC); the search box's info-icon hint calls this out. |

## Phase 2 — Tabs, panes & window management

| State | Item |
|---|---|
| Completed | Tabbed browsing with independent per-tab loading state |
| Completed | Tab pinning with automatic restore on startup |
| Completed | Tab context menu — Close, Close Others, Close to the Right, Close to the Left |
| Completed | Tab groups (named sets of paths opened as a batch) |
| Completed | Split-pane view with per-pane persistence |
| Completed | Collapsible sidebar |
| Completed | Window size modes — fullscreen, half-screen, custom dimensions |
| Completed | Window position memory in addition to size — fixed a physical/logical pixel unit mismatch (`GetClientRect` vs. egui's logical points) in the drag/resize-finished save path that inflated the remembered size on any display scaled above 100%, throwing off both size and position on the next launch |
| Completed | Maximization bounds and minimum-size constraints |
| Completed | CLI accepts `::MY_PC::`, `::RECYCLE_BIN::`, `shell:MyComputerFolder`, `shell:RecycleBinFolder` |

## Phase 3 — View modes & column system

| State | Item |
|---|---|
| Completed | Details view |
| Completed | Detail + Preview view |
| Completed | Gallery view with five thumbnail sizes |
| Completed | Columns view (Finder-style progressive browser) |
| Completed | Column + Preview view |
| Completed | Preview view (list beside a large preview pane) |
| Completed | Column show / hide, drag-resize, and reorder (left / right / start / end) |
| Completed | "Size column to fit" and "Size all columns to fit" |
| Completed | Lexicographic multi-column sorting (Shift+Click add, Ctrl+Click remove) |
| Completed | Per-directory persistence of column layout, sizing, sort, and view mode |
| Completed | Auto-fit columns on first open without overriding a manual resize |
| Completed | Recycle Bin columns (Original Directory, Deleted) included in fit-to-content measurement |
| Completed | Tag-view table column parity — fit, reorder, header context menu |
| Completed | Consistent 8px left inset and header top padding across every table view |
| Completed | Bottom padding below the last row in Details and Gallery views |

## Phase 4 — File operations & shell integration

| State | Item |
|---|---|
| Completed | Cut / Copy / Paste / Rename / Delete / Properties |
| Completed | Copy Path (Ctrl+Shift+C) |
| Completed | Native `IFileOperation`-backed operations with real Explorer progress dialogs |
| Completed | Copy / paste overwrite handling for existing files and folders |
| Completed | Delete to Recycle Bin as the default action |
| Completed | "Delete Permanently" context-menu action with the native warning dialog |
| Completed | Native Recycle Bin browsing, Restore, and Delete Permanently |
| Completed | Inline rename with Explorer-style click-pause-click gesture |
| Completed | Rename selects base filename only, excluding the extension |
| Completed | Rename selection visible on the first frame (no highlight lag) |
| Completed | Rename gesture disabled on drives / "This PC" |
| Completed | Rename support in the Preview view |
| Completed | Live invalid-character filtering while renaming |
| Completed | Drag and drop within the item viewer — the drag ghost/drop-target highlight clears the instant a drop completes (Details, Detail Preview, and Gallery views); the reset now clears `drag_state.start_pos` and `source_items` too, not just `active`, since a leftover `start_pos` re-armed the drag on the next pointer move. A same-name collision on drop now shows the app's own themed Replace/Skip/Rename modal (routed through the same robocopy conflict-check pipeline as clipboard paste) instead of a raw native `IFileOperation` "confirm replace" dialog, and dropping an item onto the empty background of the folder it's already in is a silent no-op instead of surfacing a native "source and destination are the same" error |
| Completed | Drag and drop onto breadcrumb folders and directory tabs |
| Completed | Drag and drop out to native Windows targets (Desktop, Explorer, apps) |
| Completed | Optional Windows shell context-menu integration (registry entries) |
| Completed | User-defined custom context-menu commands with custom icons |
| Completed | Auto-refresh of the file list after a custom command completes |
| In-Progress | **Shift+Delete keyboard shortcut** — code path exists and is symmetric with the working plain-Delete path, but the Shift modifier has been measured as not reaching the handler at keypress time. "Delete Permanently" in the context menu is the reliable path today. *Next: root-cause the egui/winit modifier propagation, or document it as a platform limitation.* |
| Completed | **Top-right notification bell + operation history panel** — bell button with an in-progress badge, click-to-open panel listing copy/move/delete operations (icon, item count, destination, status, progress bar), a bottom-right completion toast, and Clear/Clear All (works on in-progress rows too - clears the row from view without touching the underlying operation). All status colors follow the user's own accent (`palette.primary`), not fixed literals. |
| Completed | **Robocopy-backed copy/move/paste engine (Phase B)** — clipboard copy and cut-move (Ctrl+C/X + Ctrl+V) now run through `robocopy.exe` (`core::robocopy`) instead of `IFileOperation`: multi-threaded (`/MT:8`), metadata-preserving (`/COPY:DAT`, `/DCOPY:DAT` for folders), with real Pause/Resume/Cancel controls and a live byte-based progress bar in the notification panel. Pause kills the current job's process (robocopy has no true mid-file pause); Resume re-launches against the same remaining-job queue with `/Z` (restartable mode), so an interrupted file resumes rather than restarting. A name-collision prompt (Replace/Skip Existing/Rename/Cancel) fills the gap robocopy itself doesn't cover (no interactive overwrite dialog of its own) - checked before the job even starts, listing every colliding name; Rename computes the next free `<name>-001`, `-002`, ... and stages the copy in a throwaway subdirectory first so the pre-existing colliding file is never touched (see CLAUDE.md - an earlier version copied under the original name directly into the target dir first, silently overwriting the file "Rename" was supposed to preserve). Verified live: a 5.2 GB / 2000-file cross-drive copy (accurate progress, working Pause/Cancel), and repeated conflict-modal runs (Replace/Skip confirmed via file-system checks). Delete still goes through `IFileOperation`/Recycle Bin, per the original architecture decision - robocopy has no recycle-bin equivalent. Drag-and-drop moves within the item viewer now also route through this same robocopy + conflict-check pipeline (see the drag-and-drop entry above); the external-file-drop dialog is still unconverted and uses `IFileOperation`. |
| Completed | **Double-click empty item-viewer area navigates up** — the existing "double-click background to go up" behavior (Details/Detail Preview views) only ever registered when the folder had visible items, since its handler lived inside the same code branch as the file table itself; an empty folder skipped that branch entirely and silently ignored the double-click. Fixed for Details and Detail Preview; Gallery/Columns/Preview views don't have the `tabbar_action` plumbing this needs yet and are a follow-up. |
| Completed | **Custom Context Menu: unquoted-path placeholder (`%L`)** — user report: a custom command (`cmd.exe /c echo %1| clip`, to copy a bare path to the clipboard) always produced a quoted path (`echo "C:\...\file.txt"| clip`) with no way to turn that off. Root-caused to `substitute()` (`core/context_menu_settings.rs`): `%1`/`%V` were unconditionally run through a `quote()` helper with no template-level opt-out, unlike real Windows shell verb substitution, which distinguishes an always-quoted `%1` from an unquoted "long path" token (`%L`) specifically for commands that want to add their own quoting or none at all. Added that same `%L` token (substituted raw, no quotes) alongside the existing `%1`/`%V`/`%*`, rather than changing `%1`'s existing quoted behavior (which other saved commands may already depend on for paths with spaces). Updated the Arguments field's tooltip to document all four tokens, across all 6 locales. 2 new unit tests (`substitute_tests`) cover the quoted-vs-unquoted distinction and `%*`'s per-item quoting directly, since this is pure string logic with no UI dependency. `cargo test` (97/97) and the i18n sync check both pass. |
| Completed | **Custom Context Menu: export/import just the custom commands** — user request: back up/share only the custom right-click commands, separate from a full settings export, while confirming the existing full export/import still carries them too (it already does - `custom_context_menu` lives on `AppSettings`, which `SettingsExportBundle` already clones wholesale). Added a new `ContextMenuExportBundle { format_version, entries }` (`core/context_menu_settings.rs`) and two new `SettingsAction::{Export,Import}ContextMenu` variants, handled in `mainwindow_imp.rs` via the exact same `rfd::FileDialog` + `serde_json` pattern the existing full `ExportSettings`/`ImportSettings` already use - just serializing/deserializing the one `Vec<CustomContextMenuEntry>` instead of the whole bundle. Import replaces the current list wholesale (matching how a full settings import already replaces `AppSettings` wholesale) rather than merging, for consistent behavior with the existing convention. New "Export"/"Import" button row added right below the existing "+ Add Command"/"+ Add Submenu" row in the Custom Context Menu settings page. A direct unit test (`context_menu_export_bundle_round_trips_through_json`) exercises the actual new code path (the `serde_json` round-trip through the new bundle type, including nested submenu children) independent of the file-dialog/disk-write plumbing around it, which is the already-proven pattern. 4 new i18n keys across all 6 locales. `cargo test` (98/98) and the i18n sync check both pass. **Live-verified partially**: confirmed the Export/Import buttons render in the right place next to the user's own real custom commands (Edit With Notepad, File Manage, Image Convert, etc.); a live click-through of the actual save dialog could not be conclusively verified this session due to a concurrent-user-interaction ambiguity (a coincidentally-named, unrelated Notepad++ window briefly became foreground during the test) - backed instead by the deterministic unit test plus the fact that the file-dialog/serialization plumbing is a line-for-line copy of the already-shipped, already-working `ExportSettings`/`ImportSettings` handlers. |
| Completed | **Settings page polish: Appearance footer padding, Tags page title, and two real spacing bugs found and fixed along the way** — three small user requests handled together. (1) Added top/bottom padding around the Appearance page's Export/Import/Reset Theme button row (previously flush against the content above and cut off tight at the bottom of the scroll area). (2) The Tags settings page (`containers/tags.rs::draw_tags`) had no page title at all, unlike every other settings page (General, Favorites, Custom Context Menu, etc., all built on the same `setting_label` + tooltip-icon pattern) - added one. (3) Adding that title surfaced two genuine, pre-existing layout bugs once the page's vertical rhythm actually got tested by eye: the very first tag group's own bordered card had an invisible top edge, and every group's border directly touched the next group's border with no gap at all (confirmed via pixel sampling: a 1px overlap, not just "thin"). Root-caused precisely: a `ui.add_space(-4.0)` at the top of the group list (originally added to flush the first group up against the sidebar before this page had a title above it) pushed the first card's frame slightly past the scroll area's own clip bounds, clipping its top border off entirely - the same class of bug as an earlier session's address-bar border investigation (a border drawn flush against/past a clip boundary loses that edge). Separately, and independently, a `ui.add_space(-12.0); // remove vertical margins between tag groups` was placed after every group specifically to *cancel out* whatever gap `item_spacing.y` would have produced, netting to a ~0px gap between cards regardless of how large `item_spacing.y` was set. Fixed by replacing the negative top offset with a real `4.0` top padding, and deleting the vertical-margin-cancelling line outright so the (already-present) `item_spacing.y = 12.0` actually takes effect. Verified with pixel sampling before/after: the gap between two real tag groups' borders went from 1px (touching) to 13px. `cargo test` (98/98) and the i18n sync check both pass. **Live-verified**: zoomed crop confirmed the first group's top border now renders as a complete, visible line; pixel-sampled the gap between two groups directly rather than eyeballing it. |
| Completed | **Sidebar tag icon: genuinely filled, and smaller** — user request. The sidebar's tag-group icons were already coded as `egui_phosphor::fill::TAG`, but still rendered as a hollow outline - root-caused to `regular::TAG` and `fill::TAG` being the *exact same Unicode codepoint* in `egui_phosphor` (confirmed by reading the crate's own source: both constants are `"\u{E478}"`), with the two weights only distinguished by which *font* resolves that codepoint. The "Fill" Phosphor font is registered under its own named family (`FontFamily::Name("phosphor_fill")`, `core/utils/fonts.rs`) - never merged into `FontFamily::Proportional` - but `draw_sidebar_virtual_item` (`containers/sidebar.rs`, shared by both the Tags and Saved Searches sidebar lists) hardcoded `FontFamily::Proportional` for every icon glyph regardless of which constant was passed in, so the codepoint always resolved to the Regular-weight glyph no matter which module the caller imported it from. Added `icon_font_family`/`icon_size` parameters to `draw_sidebar_virtual_item` so each caller controls both explicitly: the Tags call site now passes `FontFamily::Name("phosphor_fill")` and a smaller `palette.sidebar_icon_size * 0.75`, while Saved Searches keeps its previous `Proportional` family and full size unchanged. `cargo test` (98/98) passes; no i18n changes. **Live-verified with a zoomed crop**: the tag icons next to "Mytag"/"YourTag" in the sidebar now render as solid, smaller filled tag shapes instead of the previous larger hollow outlines. |

## Phase 5 — Preview system

| State | Item |
|---|---|
| Completed | Plain text preview with encoding sniffing |
| Completed | Text extraction preview for PDF and Word (.docx) |
| Completed | Rendered Markdown — headings, emphasis, lists, code, quotes, rules, task lists, simple tables |
| Completed | **Mermaid diagram rendering in Markdown preview** — fenced ` ```mermaid ` code blocks render as real diagrams instead of plain code, scoped to flowcharts (`graph`/`flowchart` TD/LR/BT/RL, box/rounded/diamond/circle node shapes, solid/dashed edges with optional labels) and sequence diagrams (participants incl. `as` aliases, solid/dashed arrows, optional arrowheads). Hand-rolled regex parsing + a from-scratch layout (Kahn's-algorithm topological ranking for flowcharts, with a cycle-safe fallback; fixed-column lifelines for sequence diagrams) painted directly via `egui::Painter` primitives (`core/mermaid.rs`) - no embedded browser/JS runtime, consistent with this app's architecture. A block that fails to parse (unsupported diagram type, malformed syntax) falls back to the existing plain code-block rendering rather than vanishing. |
| Completed | Image preview |
| Completed | Animated GIF playback |
| Completed | Video playback with play / pause and seek slider |
| Completed | Zip-format archive listing with nesting and per-entry sizes |
| Completed | **SVG preview** — rasterized via `resvg`/`fontdb` and shown as an image, same as any raster format (this was already fully implemented in `core/preview.rs`'s `load_svg_preview` before this entry existed - the backlog/README simply hadn't been updated to reflect it). `.svgz` (gzip-compressed SVG) is also supported now: the loader previously routed `.svgz` through the same code path as plain `.svg` with no decompression step, so it silently failed to parse - fixed by gzip-decoding first via `flate2` (already present transitively via other dependencies, so this adds it as a direct dependency without adding a new crate to the build). |
| Completed | Text selection with right-click to copy in the text preview |
| In-Progress | **Markdown preview copy fidelity** — right-click copy works, but the paragraph under the pointer contributes its whole text rather than only the selected portion (egui collapses label selection on any button press). See `PRD.md` §7.2 and the doc comments in `itemviewer_preview.rs`. *Next: decide whether a custom rich-text widget is worth the cost, or accept permanently and document in user-facing docs.* |

## Phase 6 — Organization (favorites, tags, groups)

| State | Item |
|---|---|
| Completed | Favorites with drag-and-drop reordering |
| Completed | Favorites management page — rename, delete, change icon, change target, reorder |
| Completed | Custom favorite / context-menu icons from image files, copied into app data |
| Completed | Searchable Phosphor icon picker (1,500+ glyphs) |
| Completed | Tags with custom colors |
| Completed | Tagged items tinted in the file list by tag color |
| Completed | Tags sidebar section; clicking a tag opens or reuses a dedicated tab |
| Completed | Tag view lists Name / Type / Location / Size / Modified |
| Completed | Reorderable tags — drag in Settings, reflected live in the sidebar |
| Completed | Custom folder colors |
| Completed | **Multi-tagging** — an item can belong to more than one tag group at once (the previous "adding to a group silently strips every other tag" exclusivity rule in `TagsState::add_paths_to_group`/`create_group_and_add` was removed; `Vec<TagGroup>` storage already supported multi-membership, so no `tags.bin` migration was needed). The tag picker (`draw_tag_picker_popup`) is now a multi-select toggle list — each group shows a checked/unchecked state via `toggle_group_for_paths` and the popup stays open across multiple picks instead of closing after the first. A new **Tags column** (`ItemViewerHeaderColumn::Tags`, appended after Created in the file view only — not shown in Drive/Recycle Bin view) renders every tag on a row as a small colored chip (`handle_draw_col_tags`), with a "+N" overflow chip once they don't fit the column width, reusing the picker's own fill/stroke color styling. Right-clicking "Remove Tag" while browsing inside a specific tag's own folder view (`draw_tag_view`, and the Settings > Tags page's inline per-group list) now removes the item from *that tag only* (new `ItemViewerContextAction::RemoveTagFromGroup(group_id, paths)` → `TagsState::remove_paths_from_group`), while the main item viewer's "Remove Tag" (no specific-tag context) still clears every tag, unchanged. 4 new unit tests cover multi-membership, the toggle helper, `tags_for_path`, and scoped removal. Live-verified: tagged one scratch file with two new scratch tag groups via the picker (popup stayed open across both toggles), confirmed it appeared in both tag groups' own views and the Tags column showed both chips with "+1" overflow, confirmed "Remove Tag" from inside one tag's own view removed it from only that tag (Tags column then showed the remaining one), confirmed the existing single-color row tint still renders. AND/OR tag filtering remains a separate, deferred follow-up (below). **Follow-up fix (same phase):** the very first version of `handle_draw_col_tags` sized a chip's pill exactly to its own text at the row's normal font size with no upper bound, so a long tag name's pill (and text) could exceed the column's width entirely for a single-tag row (the "+N" overflow chip only ever covers the *second-and-later* tag in a row, never the first). Fixed by giving chips their own smaller font (`font_id.size - 2.0`, floor 9.0 - both more compact and less prone to overflowing in the first place) and capping any one chip's text at 90px, truncated with an ellipsis via the existing `truncate_item_text` helper (the same one `draw_table_text` already uses) when a name doesn't fit - so a chip's pill can now never be wider than the text budget it was measured against. Live-verified with a deliberately long tag name ("Really Long Test Tag Name Here"): renders as "Really Long Test …" cleanly inside its own pill, no overflow. **Second follow-up fix:** the Tags column was missing entirely from Search results - `visible_order`'s `is_search_view` branch is a separately hardcoded column list (`Name`/`OriginalDirectory`/`Type`/`Size`/`Modified`) that predates Tags and was never updated when Tags was added to the normal file view's own list; this was an oversight, not an intentional exclusion (search results, like the tag view, list items from many different real folders, so tags are equally relevant there). Fixed by adding `Tags` to that list and by fixing `compute_item_viewer_column_layout`'s search-view early-return branch, which had hardcoded `tags_width: 0.0` (from before the Tags column existed) instead of using the already-computed `widths.tags_width`. Live-verified: a scoped search now shows the Tags column with the correct chip for a tagged scratch file. **Follow-up (visual polish):** the Settings > Tags page's per-group section was previously a bare transparent `Frame::NONE` with no background/border at all, reading as flat/unstyled next to every other card-like surface in the app. Each group is now a real card (`palette.faint_bg_color` fill, a border tinted from the group's own color at low alpha, rounded corners) with a colored tag glyph next to the group name and the item count restyled from a plain `"(N)"` label into a small colored pill - both reusing the group's own color so the card, icon, and count all visually agree at a glance. The collapsible body's own separate colored border/fill was removed (it read as a second nested box once the new outer card existed) in favor of a plain divider line, avoiding a "box inside a box" look. Not re-verified live this session (see the click-unresponsive note on the theming entry above); backed by code review and a clean build/test pass - the same `egui::Frame`/color-picker patterns already used successfully elsewhere in this file. |
| Completed | **"Open File Location" context menu entry** — right-clicking a file/folder in the Tag view (`draw_tag_view`, and the Settings > Tags page's inline per-group list) or a Search results tab now shows an "Open File Location" entry that opens the item's containing folder (`path.parent()`) in a new tab, reusing the existing `ItemViewerAction::OpenInNewTab` - since both views show items from many different real folders (unlike a normal listing, where the containing folder is just the tab's own current directory), this is the only easy way to jump to where a result actually lives. Gated by a new `is_search_view: bool` parameter on `handle_context_menu_actions` (computed from `parse_search_view_path` at each of its 3 call sites - itemviewer.rs already had it in scope, itemviewer_gallery.rs and itemviewer_columns.rs compute or hardcode it) so the entry does **not** appear for a normal folder view, where it would be redundant; `handle_context_menu_actions_tags` (tags.rs) shows it unconditionally, since that function is only ever used inside a tag's own view. New i18n key `inputs_open_location`, added to all 6 locales. Live-verified: appears correctly in both the Tag view's and a scoped Search results tab's context menus (and correctly absent from a normal folder's), clicking it opens the exact containing folder in a new tab in both cases. |
| Pending | Multi-tag AND/OR filtering in the tag view (depends on multi-tagging) |

## Phase 7 — Theming & appearance

| State | Item |
|---|---|
| Completed | Dark / light mode toggle |
| Completed | Full color palette editor |
| Completed | Accent-driven retinting across sidebar, explorer, list, tabs, toolbar, borders |
| Completed | Thicker accent-colored borders, including an outer window border |
| Completed | Application font selection from system-installed fonts |
| Completed | Standardized settings form styling across every Settings page |
| Completed | **Enhanced theme customization** — three sub-phases: (1) granular color overrides, (2) custom layout options, (3) prebuilt alternate themes. **Phase 1 (granular color overrides) done.** The Appearance settings page (`customizetheme.rs`) previously exposed only 6 of `ThemePalette`'s ~45 color fields (the "Core Colors" grid) - everything else was only reachable by hand-editing an exported theme JSON. Added six new picker sections (Text & Rows, Panels & Icons, Tabs, Drive Usage, Checkboxes & Buttons, Corner Radius) covering every previously-unexposed field, via a new `color_row` helper (label + right-aligned color picker + `end_row()`, extracted from the repeated shape the existing Core Colors rows already hand-wrote) so ~48 new rows didn't need ~10 lines of boilerplate each. Also promoted colors that were fully hardcoded per-mode literals inside `apply_theme` (not real fields at all) into six new `ThemePalette` fields with pickers - `faint_bg_color`, `input_field_bg` (shared by a `TextEdit`'s own background and every other input widget's background, which were already kept equal to each other), `widget_text_inactive`/`widget_text_hovered`/`widget_text_active` (a widget's own text color in each interaction state), and `selection_text_color` (the color text is repainted in when highlighted) - each with a `default_xxx()` fallback matching its previous hardcoded value, so existing users see zero visual change until they touch a new picker. Also wired `widgets.noninteractive.fg_stroke.color` (a color that was hardcoded per-mode but coincidentally already matched the existing `text_normal` field's own default in both modes) to reuse `text_normal` directly instead of adding a redundant new field. **Bug fix (found during this work):** removed the "Context Menu Text Size" control - it was wired to `editing_palette.text_size`, the exact same field the "Text Size" control right above it already edits (no separate context-menu text size concept exists anywhere else in the codebase), so it silently duplicated the field above it rather than controlling anything distinct. New fields are all `#[serde(default = "default_xxx")]` and appended at the literal end of `ThemePalette` (no `THEME_VERSION` bump needed, so no existing user's saved theme is reset). 48 new i18n keys added across all 6 locales (plus removal of the now-dead `theme_contextmenu_textsize` key); `cargo test` (79/79) and the i18n sync check both pass. **Verification note:** live UI verification was not completed this session - the app kept losing synthetic-input focus back to the agent's own terminal/desktop window immediately after each click (consistent with the user being actively at the keyboard at the time), so clicking through to the Appearance page and confirming each new section renders/edits correctly is still pending a session where that contention isn't present. The change is otherwise low-risk: every new field follows an identical, already-proven pattern (the same `color_picker_control`/`rgba_color_edit_button` call already used successfully by the 6 existing Core Colors rows), and a clean build + full test pass were confirmed. **Phase 2 (custom layout options) done.** Sidebar width was previously ephemeral - drag-resizable (`mainwindow.rs`) but reset to a hardcoded 250.0 on every restart, never persisted anywhere. Added `sidebar_width: f32` to `core::indexer::SidebarSectionsSnapshot` (the existing `sidebar_sections.bin` file that already persists which sidebar sections are expanded - a natural fit rather than a new file, appended at the end per the postcard positional-format rule), saved both on drag-release (`separator_response.drag_stopped()` in `mainwindow.rs`) and whenever a section is expanded/collapsed (the existing save call in `sidebar.rs`, updated to include the field so it doesn't get silently reset to default by an unrelated save), and loaded at startup into `sidebar_state.sidebar_default_width` instead of the old hardcoded value. New "Layout" section in the Appearance settings page (`customizetheme.rs`, below Typography): three Row Density preset buttons (Compact/Comfortable/Spacious) that set `row_height` + `sidebar_item_spacing_y` together to tuned combinations - pure convenience over two fields that already existed, no schema change needed for that part - plus a Sidebar Width `DragValue` bound to a new `ThemeCustomizer.sidebar_width` draft field (loaded once from `load_sidebar_sections()` when the customizer is constructed, mirroring how `light_palette`/`dark_palette` are already draft copies), emitting a new `ThemeCustomizerAction::SidebarWidthChanged(f32)` handled in `mainwindow_imp.rs` by updating `sidebar_state` and persisting the full snapshot. 2 new unit tests cover the new field directly: a round-trip through postcard, and (a genuine, non-obvious finding written up in `CLAUDE.md`) that an old save file missing this trailing field doesn't get gracefully per-field-defaulted the way `#[serde(default = ...)]` might suggest - postcard's format has no "end of struct" marker, so the whole decode fails and `load_sidebar_sections`'s own `.unwrap_or_default()` fallback is what actually saves it, resetting every field to default rather than just the new one. `cargo test` (81/81) and the i18n sync check both pass. **Verification note:** live UI verification was not completed this session - after a one-off ~25 second slow app startup (confirmed not a hang - the window existed throughout and became responsive on its own; likely antivirus scanning the freshly-built binary, not a code issue), synthetic clicks aimed at the Settings page's "Appearance" category button repeatedly landed on the adjacent "Startup & Window" button instead, across 5 retries with no change in outcome - the same general click-targeting flakiness observed elsewhere this session, not something specific to this change. **Phase 3 (prebuilt alternate themes) done.** Added a "Prebuilt Themes" section (`customizetheme.rs`) above the existing "Color Scheme" accent row - the bigger-grained choice sits above the finer one. Six named, color-only presets (Default/Midnight/Ocean/Forest/Sunset/Slate, `PALETTE_PRESETS` in `theme.rs`) each bundle an accent plus two purely-decorative curated colors (the pinned-tab star and favorite-star fill) - deliberately *not* the semantic drive-usage warning/critical colors or tab-close-hover, so a preset can never quietly change what a color *means* elsewhere in the app, only its brand feel. `apply_theme_preset` sets those three fields then calls the same `regenerate_base_derived_colors` a manual accent edit already triggers, so a preset is just a bundle of the exact same edits a user could make by hand, not a separate code path - it round-trips through Export/Import/Reset for free since it never introduces new schema, only reuses existing `ThemePalette` fields. Each swatch button is hand-painted (two color halves + a selection outline) rather than a flat single-color square like the accent presets, so the "which colors would this apply" preview doesn't require a tooltip or a click to find out. 7 new i18n keys (`theme_prebuilt_themes` + 6 preset names) added across all 6 locales; i18n sync check passes (the 6 preset-name keys are only reachable via `.tr(preset.name_key)`, the same dynamic-lookup shape the pre-existing `theme_scheme_*` accent presets already use, so the checker's static-analysis correctly flags them as reachable-but-not-statically-provable rather than genuinely dead). `cargo test` (88/88) passes. **Live-verified**: clicking "Ocean" retinted the entire running app (tab strip, active-page highlight, Core Colors' Primary/Primary Active/Borders Active swatches) to teal in one click and the Ocean swatch showed the selected-state outline; clicking "Default" afterward correctly restored the original purple accent - confirming the preset-application code works end-to-end, not just in isolation. **Follow-up: presets now also drive button color, and expanded from 6 to 11 colors.** `button_background`/`button_stroke` were two long-standing `ThemePalette` fields editable in the "Checkboxes & Buttons" Core Colors section with **no call site anywhere that actually read them** - editing them, or switching a preset, visibly did nothing to any button in the app. Fixed by (1) folding both into `regenerate_base_derived_colors` (a new `tint_alpha` helper tints a translucent neutral's RGB toward the accent while preserving its original alpha, since these are semi-transparent grays, not opaque surfaces) and (2) wiring `eden_button` (the general-purpose button used by Export/Import/Reset Theme and most other plain action buttons app-wide) to actually paint with `palette.button_background`/`button_stroke` instead of leaving it to the ambient neutral style. Five more presets added (Crimson/Magenta/Violet/Indigo/Skyline, the last pairing a blue accent with a yellow pinned-tab/favorite highlight) for 11 total, covering warm/cool/neutral families the original 6 didn't. `cargo test` (88/88) and i18n sync check both pass. **Verification note:** live-verified earlier in the same session (the Ocean/Default test above, run before this follow-up); the follow-up's own button/preset-expansion changes were not re-verified live - a later click-testing attempt in this session found the app's mouse input (and, this time, keyboard Tab focus too) completely unresponsive across a fresh relaunch, matching this file's own recorded "mouse-click-based live UI testing can go completely unresponsive for an entire session" gotcha - confirmed via a cheap control click before giving up, per that gotcha's own advice, rather than burning further retries. Backed by a careful code review (the new `tint_alpha` call sites, `eden_button`'s new explicit fill/stroke, and the 5 new preset entries all follow the exact shape of already-proven, already-live-verified code next to them) plus the clean build/test pass. **Follow-up: added a 12th preset, "Horizon"** (blue accent + orange pinned-tab/favorite highlight, matching a reference blue-to-orange gradient image the user provided) - same shape as every other preset entry, no new mechanism. `cargo test` (88/88) and i18n sync check pass. **Live-verified this time** - clicks had recovered from the unresponsive state noted just above: opened Settings > Appearance, clicked the new Horizon swatch, and confirmed it applied (selection outline moved to it, the whole app's accent retinted to its blue). **Follow-up: the "secondary" color barely touched anything a user would actually see** (user feedback: "i dont see it apply to any significant ui component") - true. Auditing every call site of `pinned_tab_color`/`button_favorite_fill` found exactly two: a pinned tab's own icon glyph, and the navbar's favorite-folder star - both tiny, both only visible in specific states (a pinned tab exists / the current folder is favorited). Fixed with a real two-color design system rather than a cosmetic tweak: (1) added a genuinely new `secondary_accent: ThemePalette` field (postcard-appended, `#[serde(default)]`, zero visual change for existing users until a preset is applied) that `regenerate_base_derived_colors` now uses to tint `toolbar_icon_color` (the whole toolbar's icon row - back/forward/refresh/new-folder/favorite/search/etc, visible on every single screen) instead of tinting it from the accent like every other icon does; (2) replaced each preset's `pinned_tab_color`+`button_favorite_fill` pair (previously identical values duplicated across two fields) with one `secondary` field feeding `secondary_accent` + both existing small accents, so the same hue now echoes in three places instead of two; (3) **re-picked all 12 presets' secondary colors** against real color-design principles (documented at length in `theme.rs`'s `ThemePresetDef` doc comment) - 8 of the original 12 had picked their "secondary" as just a paler/darker tint of the *same* hue as the accent (e.g. Midnight's icy-blue secondary was really just a lighter blue, not a second color at all), which is why the effect was invisible even conceptually. Every preset's accent/secondary pair now differs in hue (Complementary Contrast - e.g. Sunset's warm orange accent pairs with a cool sky-blue secondary), temperature (a cool accent always gets a warm secondary and vice versa), and luma/value (a 25+ point 0-255 brightness gap, so the two halves of a preset's own preview swatch read as genuinely distinct rather than a same-brightness blob), while keeping matched saturation/tone so the pair still reads as chosen-together rather than random (Analogous Ties). Structurally this follows the 60-30-10 rule: accent dominates large surfaces + ordinary icons (60%), secondary now owns the toolbar's full icon row (30% - real coverage, not a token touch), and the two small pre-existing accents remain the rare 10% pop - each tier deliberately owns a different *category* of element (Asymmetrical Placement / Visual Weight) instead of both colors competing for the same spot. `cargo test` (88/88) and i18n sync check both pass (no i18n changes needed - preset names are unchanged, only their color values and the field name backing them). **Live-verified**: applying Slate (a neutral gray accent) visibly retinted the entire toolbar's icon row to a distinct amber, clearly separate from the app's own gray accent everywhere else - confirming the secondary color is now genuinely significant rather than a two-pixel afterthought. **Follow-up: button background/stroke now tint from the secondary color (not the accent), plus a new customizable Button Text color, plus themed the destructive/dialog buttons that were still bypassing this system.** Buttons are frequent, large-footprint UI (Settings, every dialog, confirmation popups) - tying them to `secondary_accent` reinforces it as a real "30% tier" alongside the toolbar, rather than splitting theme identity three ways. Added `button_text_color` (new field, a light secondary tint mirroring how `icon_color` is a light accent tint) so `eden_button`'s text also carries the secondary hue without sacrificing legibility; exposed as a new "Button Text" Core Colors row. Audited every raw `ui.button(...)`/`egui::Button::new(...)` call site for destructive/dialog contexts specifically (the user's own examples: "clearing list", "warning on destructive operation") and found two real gaps - the sidebar's "Clear All" recent-locations action and the Tags page's delete-group confirmation (both plain unthemed buttons, the confirmation with no visual weight distinguishing its destructive "OK" from "Close") - fixed by routing them through `eden_button`/`primary_dialog_button`/`ghost_dialog_button` like every other themed button in the app. **Live-verified**: opened the color picker on a Core Color swatch and confirmed the new Hex/HSL fields render correctly alongside egui's existing RGB fields and 2D picker, reading the right values for the swatch's current color. |
| Completed | **Custom color picker with hex/RGB/HSL input** — user request: "display hex value, hsl, rgb also when user select color with picker. And user can input hex value, rgb, hsl to select color." egui's built-in color-picker popup (used by every Core Colors row via `rgba_color_edit_button`) only ever offered a 2D saturation/value square, a hue slider, and RGB drag-values - no hex field and no HSL fields at all (confirmed by reading egui 0.35's own source: its HSL-adjacent sliders are literally `if false { ... }`-gated dead code upstream). Rebuilt `rgba_color_edit_button` (`core/utils/widgets.rs`) as its own swatch-button-plus-popup (mirroring egui's internal `color_edit_button_hsva` shape closely enough to keep its exact click/hover visuals) so a custom popup body could be inserted: egui's own `color_picker_color32` (square + hue slider + RGB) first, then a new Hex row (a `TextEdit` showing/accepting "RRGGBB", with or without a leading `#`) and a new HSL row (three `DragValue`s: 0-360°, 0-100%, 0-100%) - both read from and write back to the same `Color32`, so all four representations (square, RGB, hex, HSL) always stay in sync with whichever one the user last touched. New `rgb_to_hsl` (`core/utils/colors.rs`, the reverse of the already-existing `hsl_to_color32`) and `parse_hex_rgb` (accepts/rejects malformed input gracefully - a still-being-typed hex string just doesn't update the color yet rather than erroring). 6 new unit tests (HSL round-trip across a range of hues, known-color HSL values, grayscale's zero-saturation edge case, hex parsing with/without `#`/case-insensitivity/rejection of malformed input). `cargo test` (94/94) passes. **Live-verified**: opened a Core Colors swatch's picker and confirmed the Hex field showed "F5AA3C" and the HSL fields showed "35.68°/90.24%/59.80%" - both correctly matching the swatch's actual color (drive-usage warning orange) - alongside egui's own square/hue-slider/RGB controls rendering normally. |
| Completed | **Address bar / search box border consistency, plus larger icon click targets with padding** — user report: clicking into the address field changed its border in a way that looked inconsistent with the rest of the app, the toolbar/address area sometimes visually overflowed the app's own outer border, and the search box would "get messy" and resize the pane. Root-caused three separate, specific bugs rather than a vague pass: (1) **Double border on address-bar edit** - `draw_bordered_breadcrumb` already wraps the whole row in one pill-shaped border at rest (added in an earlier session specifically so the address bar "always reads as its own distinct field"), but the path-edit `TextEdit`'s own inner frame (`draw_breadcrumb_row_contents`) *unconditionally* drew a second border around itself in a different, hardcoded corner radius (4, ignoring `palette.small_radius`) every time editing was active - so clicking in didn't just reveal the same field, it added a second, differently-rounded box nested inside the first. Fixed by only drawing that inner border for its one actual purpose (flagging an invalid path in red) and matching `palette.small_radius` when it does, so browsing and editing now share exactly one border. (2) **Search box scope toggle causing pane resize** - the "This folder"/"Everywhere" toggle was two `selectable_label` text buttons whose combined width varies a lot by the active language's translation length; inside a fixed-height toolbar row (in a split pane, or a longer language), that variability was exactly what pushed the row onto a second line, which - since the row's height budget doesn't grow to match - visibly resized/clipped the surrounding layout. Replaced with two fixed-size icon toggles (folder icon / globe icon, `clickable_active_icon`) carrying the same translated text as a tooltip instead of as visible label width, so the row's width is now constant regardless of language. (3) **Icons had zero padding around their own click target** - `clickable_icon_sized_with_base_color` and `clickable_active_icon` (the two helpers behind nearly every icon button in the app: toolbar, breadcrumb, context menus, the new search-scope toggle) allocated a clickable rect sized to *exactly* the glyph's own rendered ink, with no reserved space around it at all - adjacent icons had no breathing room beyond a layout's generic `item_spacing`, and each one's hit target was only as forgiving as its own thin glyph shape. Added a shared `ICON_CLICK_PADDING` (4px on all sides) to both helpers, plus a soft rounded hover/active background fill so the added space reads as a real button rather than empty dead space. `cargo test` (94/94) and i18n sync check both pass. **Live-verified**: entering address-bar edit mode now shows exactly one border (previously two); the search box's scope toggle renders as two icon buttons with a clear active-state highlight on one line, matching the fix. **Follow-up (same day, user re-reported with screenshots): the gap/border were still too subtle in browse mode, the search box's bottom border was outright vanishing, and the search icon didn't act as a toggle.** (1) The address-bar pill's right-side gap (`right_gap`, `draw_bordered_breadcrumb`) was 8px, which read as "almost touching" next to the app's own thick accent-colored outer window border - widened to 16px. (2) The pill's own border stroke used `palette.borders_default` at its normal, deliberately-faint alpha (meant for subtle dividers elsewhere) - now doubles that alpha for just this one field, since an address bar is something a user actively looks for and clicks into, not a background decoration. (3) **Root-caused the vanishing bottom border as a genuine regression from this same entry's own icon-padding fix**: `clickable_active_icon` (used by the search box's new folder/globe scope toggle) got 8px taller once padding was added, but the toolbar row's own height budget (`tabbar_height` in `explorer.rs`, a hardcoded `Size::exact(...)` shared by every row) was never adjusted to match - so switching a pane into search mode needed more vertical room than the fixed budget allowed, and the file list starting right where that budget said the row should end painted over the bottom edge of the (now slightly too-tall) pill border. Fixed by adding the same 8px back to `tabbar_height`'s base for both single-pane and split-pane layouts. (4) The search toolbar icon always set `search_box_editing = true` with no way to toggle it back off short of Escape or Enter - `enter_search_box_edit_mode` (`mainwindow_imp.rs`) now flips it off if it's already on, and the icon itself is tinted with the accent color while active (`nav_icon_button_active`, mirroring how the view-mode icons already show their active state) so the toggle state is visible, not just functional. (5) Added two small toolbar separators (search icon | view-mode icons | terminal icon) per direct request, grouping the toolbar's three logical clusters the same way `ui.separator()` already divides the toolbar from the address bar itself. `cargo test` (94/94) and i18n sync check both pass (one new key, `tooltip_search_close`, across all 6 locales). **Live-verified**: the address bar's gap and border are both now clearly visible at rest; opening the search box shows an unbroken border on all four sides (previously clipped at the bottom) with a highlighted search icon; clicking the search icon a second time correctly returns to the normal breadcrumb instead of staying stuck in search mode. **Follow-up (user re-reported: address bar still resizes taller after clicking search, in both single- and dual-pane).** The previous fix only widened the shared *row's* height budget (`tabbar_height`) - it never made the address bar's *own* bordered pill actually commit to one height, so the pill itself still auto-sized to whatever its current content (a plain breadcrumb vs. the taller search box) naturally needed. Root-caused precisely: `draw_bordered_breadcrumb` now allocates its content through `ui.allocate_ui_with_layout(vec2(width, ADDRESS_BAR_CONTENT_HEIGHT), ...)` - a fixed size - but egui's own documented behavior for that call is "if the contents overflow, more space will be allocated" (confirmed directly in its source), so a taller child still grows the parent regardless of the size requested; the fixed height is only a floor, not a ceiling. Combined with a genuine second cause: the search box's own outer layout was still `ui.horizontal_wrapped` (a leftover from before its scope toggle became fixed-width icons), which could still commit to a taller wrapped line under the right width, feeding directly into that same "overflow grows the parent" behavior. Fixed both causes together - switched to plain `ui.horizontal` (a guaranteed single line now that the toggle's width is constant) and raised `ADDRESS_BAR_CONTENT_HEIGHT` to comfortably exceed either mode's real single-line content height (24 → 28) - rather than one incomplete fix layered on another. Also added real top/bottom padding to the search query box's own rounded frame (previously zero, deliberately, to avoid this exact overflow before the real fix existed) and thickened the dual-pane active-pane top-line indicator from 2px to 4px per direct request. `cargo test` (94/94) passes; no i18n changes. **Live-verified**: switching a single-pane tab into search mode and back no longer visibly changes the toolbar/address-bar row height. **Follow-up (user re-reported yet again, with the status bar visibly shifting too - the previous fix genuinely didn't hold).** Root-caused precisely this time: `ui.with_layout` (what the zero-width-spacer fix from the previous entry actually ran inside) is implemented as `scope_builder` with no `max_rect` override, so it inherits the *parent's* rect rather than being handed a fixed one - meaning the "spacer" fix was subject to the exact same "grows to fit whatever was actually drawn" behavior as `allocate_ui_with_layout` before it, just one layer removed. Every API tried so far (`set_min_height`, `allocate_ui_with_layout`, an invisible spacer inside `with_layout`) shares this same root problem: each one lets egui's own layout system decide the final size from real content, and none of them is a hard, unconditional floor. Fixed by *not* asking any layout helper to size this rect at all: `draw_bordered_breadcrumb` now calls `ui.allocate_exact_size` once for the *entire* bordered pill (an unconditional, exact allocation - the same primitive icon buttons already rely on for a real fixed size), paints the border itself directly via the painter, and hands the breadcrumb/search content a `ui.new_child` whose `max_rect` is explicitly fixed to the remaining interior rect and whose `clip_rect` matches it - so content that would have grown the row is now invisibly clipped instead. This is a fundamentally different mechanism from every previous attempt (manual allocation + explicit clipping, not "ask a layout helper nicely and hope it holds the size"), which is why it's expected to actually stick where the others didn't. Also reduced the type-to-filter query box's own top padding (2px, from 4) and thickened the dual-pane active-pane indicator per direct follow-up requests. `cargo test` (94/94) passes; no i18n changes. **Live-verified precisely this time** (compared exact pixel row position of the file list's "Name" header, not just an eyeballed "looks the same"): identical y-coordinate in both browse and search mode on the same tab, repeated twice for confirmation. **Follow-up (user re-reported the bottom border still missing, with a screenshot).** Root-caused to the row-height *budget* (`tabbar_height` in `explorer.rs`) being computed as its own independent guessed constant (the earlier "+8.0" fudge factor) rather than from the address bar's actual real size - it left only ~2px of slack around the pill once the true fixed height was accounted for precisely, an amount easily lost to rounding, which clipped just the pill's bottom edge (including its manually-painted border) without visibly affecting anything else. Fixed by exporting the address bar's real total height as `ADDRESS_BAR_TOTAL_HEIGHT` (`itemviewer_navbar.rs`) and computing `tabbar_height` *from* that constant plus a deliberate `ADDRESS_BAR_SLACK` (10px), instead of as an independent number that can silently drift out of sync with it again. Also made the type-to-filter query box's rounding/padding more pronounced (medium radius instead of small, 10px/6px margins instead of 8px/4px) after a follow-up screenshot showed it reading as barely-rounded/barely-padded. `cargo test` (94/94) passes. **Live-verified with pixel-level zoom crops this time** (not just a full screenshot glance): the search box shows a complete border on all four sides with real margin around it, and the type-to-filter box shows a clearly rounded, padded rectangle. **Follow-up (user clarified: wanted *outer* padding - space between the box and its surroundings - not more *inner* padding around its own text).** The previous fix only ever touched `inner_margin` (the gap between the border and the text inside it); added a separate `outer_margin` (a real, distinct `egui::Frame` property, not just more of the same setting) of 6px left/top and 8px bottom, leaving right at 0 since the box already reads as anchored to the view's top-left corner and padding its one open side would look like an unexplained gap rather than intentional spacing. `cargo test` (94/94) passes. **Live-verified with a zoomed crop**: the box now has a visible gap from the toolbar above, the view's left edge, and the file list header below it, in addition to its existing inner padding around the text. **Follow-up (user reported two more issues: the type-to-filter box jumps to the bottom of the view when a filter has no matches, and the address bar's bottom border was, yet again, still invisible).** (1) **Filter box relocating on empty results**: root-caused to `itemviewer.rs`'s draw order - the "folder is empty"/no-results placeholder used `ui.centered_and_justified(...)`, which (confirmed by reading its egui source) claims the *entire* remaining rect for its content and advances the parent `Ui`'s cursor to the very bottom of that claimed space; since the filter box itself is drawn by `handle_global_actions`, which the original code called *after* that empty-state check, the box was laid out into whatever sliver of vertical space was left below the message's already-claimed rect, rather than at the top. Fixed by moving the `handle_global_actions` call (and therefore the filter box) to before the empty-state check, so it claims the top of the content area first and the message centers in whatever remains below it. (2) **Bottom border, for real this time**: previous attempts had each fixed a genuine but different bug (double border, row-height budget drift, a `with_layout`/`allocate_ui_with_layout` sizing floor) without ever addressing the actual rendering defect underneath. Diagnosed properly this time with a single-variable-at-a-time isolation test: a temporary `rect_filled` over the exact same `pill_rect` painted a perfectly clean, fully-bounded rectangle in both modes, proving the `Rect` itself was never the problem; swapping the border's `rect_stroke` call between an opaque test color and the real (translucent) `pill_border` color, with every other parameter (corner radius, width, `StrokeKind`) held fixed one at a time, showed the bug reproduced *only* when the stroke color was translucent - an opaque color at the same width/radius/StrokeKind always rendered a complete border. Root cause: a thin (1.5px), semi-transparent stroke is vulnerable to anti-aliasing/sub-pixel blending making one edge (here, the bottom) render as nearly or fully invisible, independent of the rect's own correctness - a rendering artifact, not a layout bug. Fixed by pre-blending `palette.borders_default`'s translucent RGBA against the pill's own known background (`palette.input_field_bg`) into a fully **opaque** `Color32` before passing it to `rect_stroke`, removing any dependence on runtime alpha compositing; also reverted an earlier, wrong-hypothesis-based workaround (an unclipped `egui::Painter::new` constructed on the theory that an ambient clip rect was cutting off the edge, which the isolation test proved false) back to the normal `ui.painter().rect_stroke(...)` call. `cargo test` (94/94) and the i18n sync check both pass; no i18n changes. **Live-verified**: typing a nonsense filter string in a real folder kept the filter box pinned to the top with the empty-state message centered below it; a zoomed pixel-crop of the search box border showed a complete border on all four sides for the first time, replacing what had consistently been an open-bottomed "C" shape in every previous attempt. A corresponding gotcha was added to `CLAUDE.md`'s "Mistakes already made once here" section, since this is a rendering-artifact class of bug (not a layout/sizing one) worth recognizing faster next time. **Follow-up (user reported, with a screenshot, that the search box's bottom border was still missing after all of the above).** The previous fix (pre-blending the border color to opaque) turned out to have fixed a different border - the address bar's own outer pill - while leaving the search *query field's* own inner border exactly as broken as before, for a genuinely different reason. Root-caused this time with a more rigorous pixel-level methodology: a temporary solid `rect_filled` test fill located the query field's exact rendered bounds, then individual pixels were sampled at each of its four edges. The opaque-color fix from the previous entry did **not** help here - even a fully opaque, 1.5px `rect_stroke` at that exact rect still rendered with specific edges completely absent (pixel-identical to plain background, not just faint), while other edges of the same stroke rendered a correct, if subtle, border pixel. The actual cause: `draw_bordered_breadcrumb` hands the address bar's content to a child `Ui` whose `clip_rect` is set to *exactly* the available interior rect (deliberately, so real content overflow clips invisibly instead of growing the row's height - the whole point of an earlier fix in this same entry). The query field's own border draws only 1-2px inset from that same rect, so its stroke sits flush against the clip boundary - and any edge of a border drawn exactly at a clip boundary gets silently discarded, regardless of the stroke's own color/opacity. Confirmed by giving the child `Ui`'s clip rect a few extra px of slack (`content_rect.expand(3.0)`) - still tight enough to invisibly clip genuine overflow, but no longer coincident with the query field's own border - after which every edge rendered correctly. Also switched the query field's border from `Frame::stroke` to a manually painted `rect_stroke` (matching the outer pill's own approach) for consistency, and made the same fix defensively for the type-to-filter box's border in `itemviewer_helper.rs`, though that box isn't inside a clipped child and was confirmed still rendering correctly on all sides on its own. `cargo test` (94/94) and the i18n sync check both pass; no i18n changes. **Live-verified with pixel scans this time, not just a screenshot glance**: sampled the exact pixel at each of the query field's four edges and confirmed all four now show the border's anti-aliased pixel (previously the edges touching the clip boundary were pixel-for-pixel identical to plain background, not merely faint). A new `CLAUDE.md` entry documents the corrected root cause (a clip rect cutting off a border drawn flush against its own boundary) as a follow-up/correction to the previous entry's translucent-stroke diagnosis, which was real but incomplete - it fixed a different border than the one still reported broken. |
| In-Progress | **Full theme customization overhaul** — user request to make nearly every remaining UI surface (sidebar, status bar, address bar, search, settings navigation, toolbar, preview pane, notifications, buttons) genuinely palette-driven, add a user-created "custom themes" list, and fix several appearance bugs. Planned as 6 phases (see `dreamy-hopping-cat.md` plan). **Phase 1 (editor bug fixes) done.** Three concrete bugs fixed: (1) **"Light mode" button did nothing visually** - the customizer's Dark/Light toggle only ever set `customizer.selected_mode` (which palette the *editor* shows), never the live `MainWindow::theme` field that `apply_theme` actually renders from - `apply_theme_customizer_action` only sets `theme_dirty = true` when the edited mode matches the currently-*rendered* mode, so editing the inactive mode silently saved to disk with zero visible effect. Fixed with a new `ThemeCustomizerAction::SetLiveMode(ThemeMode)`, emitted by the same toggle buttons, that also flips `self.theme` and marks it dirty - clicking "Light mode" now both edits the light palette *and* actually shows it. (2) **Dead "Resize Handle" field** - its picker (`palette.resize_handle`, Panels & Icons section) had no reader anywhere; the real sidebar-drag-handle paint call (`mainwindow.rs`) read a different, oddly-placed field (`button_seperator_handle_fill`, under "Checkboxes & Buttons") instead. Repointed the paint call to `palette.resize_handle` and deleted the now-redundant `button_seperator_handle_fill` field and its picker entirely, rather than keeping two fields for one control. (3) **No app-wide separator-line color** - `apply_theme` resets `style.visuals` to egui's stock default before applying overrides but never touched `widgets.noninteractive.bg_stroke` (what every plain `ui.separator()` actually draws), so no palette field reached it at all. Added a new `separator_color` field (defaulted to egui's own stock value so this is a true no-op for existing users) and wired it in. Also: added real bottom padding under the Dark/Light mode button row (previously none - the next control started right at the row's own item-spacing); extracted the dual-pane active-indicator's two independent hardcoded `4.0` stroke-width literals into one named `ACTIVE_PANE_INDICATOR_THICKNESS` constant and bumped it to `6.0`; and gave the preview pane's filename its own small bordered box (bold, `text_size + 3.0`) with asymmetric outer margin (top/left/bottom only) instead of rendering as a plain unbordered label. 2 new i18n keys (`theme_colors_separator`, replacing the removed `theme_colors_button_separator_handle`) across all 6 locales. `cargo test` (94/94) and the i18n sync check both pass. **Live-verified**: clicking "Light Mode" in the Appearance page now genuinely retints the entire running app (confirmed via full-window screenshot, not just the editor's own preview); the new "Separator Line" swatch renders distinctly from "Resize Handle" in Panels & Icons; the mode-toggle row now has a visible gap before "Application Background" starts; and switching to Preview layout and selecting a file shows the filename in a bordered, bold, larger box. **Phase 2 (sidebar/status bar/address bar/search/settings-nav/toolbar/preview-pane fields) done.** 10 new fields added, each appended with a `default_xxx()` matching the previous hardcoded/ambient look (transparent for surfaces that were never filled at all - status bar, address bar, toolbar, preview pane - so this is a true no-op until touched): `sidebar_text_color` (sidebar item-row labels, `sidebar.rs` - two call sites both previously used ambient `ui.visuals().text_color()`), `status_bar_bg_color`/`status_bar_text_color`/`status_bar_icon_color` (`explorer.rs` - `status_counter`/`status_size` previously took one shared `color` param for both the icon and the number/size text; split into separate `text_color`/`icon_color` params), `address_bar_bg_color` (new `rect_filled` behind the address-bar pill, `itemviewer_navbar.rs` - previously the pill only ever drew its border, never a fill), `search_active_icon_bg` (the shared `clickable_active_icon` helper's active/hover background, `core/utils/widgets.rs` - previously read `primary_hover` directly with no field of its own; still re-derived from the accent by `regenerate_base_derived_colors` exactly like `primary_hover` was, so a preset/accent change still updates it automatically), `settings_nav_inactive_color` (`settings.rs` - the Settings page's own category sidebar, previously ambient), `toolbar_bg_color` (new fill behind the whole toolbar/address-bar row, `itemviewer_navbar.rs`), `toolbar_icon_disabled_color` (replaces `ui.visuals().text_color()` for a disabled nav icon), `preview_pane_bg_color` (both of the preview pane's outer frames). 5 new customizer sections (`customizetheme.rs`, using the existing `color_row` helper): "Sidebar & Navigation", "Status Bar", "Address Bar & Search", "Toolbar", "Preview Pane". 15 new i18n keys (5 section headers + 10 field labels) across all 6 locales. `cargo test` (94/94) and the i18n sync check both pass. **Live-verified the rendering, not the live edit**: scrolled to the bottom of the Appearance page and confirmed all 5 new sections render with the correct labels in the right order, and each swatch correctly previews its field's actual default value (a checkerboard pattern for every transparent-by-default field - Status Bar/Address Bar/Toolbar/Preview Pane backgrounds - versus a solid distinct color for every non-transparent one, e.g. Search Active Icon Background showing the accent-derived blue). Actually editing a swatch's color and confirming the target element updates hit this session's own previously-documented click-targeting flakiness (a hex-field edit repeatedly failed to register despite the click landing squarely on the field in every screenshot) - not re-litigated further per the same gotcha's own advice to stop retrying once confirmed. This is low-risk: every new field reuses the exact same `color_row`/`rgba_color_edit_button` mechanism already live-verified working in the prior Hex/HSL picker entry above, and every call-site wiring is a straightforward one-line substitution of an existing hardcoded/ambient color for a new palette field, structurally identical to dozens of already-verified fields before it. **Phase 3 (notification theming + semantic status colors) done.** Rewired `status_color()` (`notifications.rs`) from "every status uses the accent color" (only `Cancelled` differed, and only by falling back to `icon_color`) to genuinely distinct, user-overridable semantic colors: `Completed` → `notification_status_success` (green), `InProgress`/`Paused` → `notification_status_warning` (yellow/orange), `Failed`/`Cancelled` → `notification_status_error` (red) - `_info` (blue) has no current call site (there's no plain "info" toast anywhere in this codebase, only `FileOpStatus`-backed ones) but is still a real, independently editable field for future use, per this session's confirmed mapping decision. Also added `notification_border_color`/`notification_bg_color`/`notification_header_text_color`, wired into both the dropdown panel (`Frame::popup`, previously ambient/ `ui.style()` defaults) and the toast (previously read `borders_default`/`extreme_bg_color`/ambient `text_color()` directly, with the toast's own fade-alpha `gamma_multiply` logic preserved exactly). New "Notifications" customizer section, 8 new i18n keys across all 6 locales. Since this is the first color-scheme fields where "match the old hardcoded look" doesn't apply (the whole point is to stop looking like a single accent color), a direct unit test (`status_color_tests`) was added instead of relying on a visual diff, asserting the exact enum→field mapping against the default palette - a more reliable check for pure logic than a screenshot. `cargo test` (95/95, the new test included) and the i18n sync check both pass. **Verification note**: live UI verification hit this session's own documented click/scroll-input flakiness again (a real file-operation toast couldn't be triggered - address-bar navigation to a scratch folder silently failed to commit across several attempts with different paths, and a synthetic mouse-wheel scroll to the new Notifications section worked once earlier in the session but stopped responding on a later attempt) - not re-litigated further per that gotcha's own advice. Backed by the new deterministic unit test (which exercises the actual logic change directly, unlike a screenshot) plus a clean build/test pass; the border/background/header wiring itself is a straightforward one-line substitution following the exact pattern already live-verified in Phases 1-2. **Phase 4 (button color completeness) done.** `primary_dialog_button` (`core/utils/widgets.rs`) previously hardcoded its text to plain `Color32::WHITE` regardless of theme - replaced with a new `primary_button_text_color` field (defaulted to white, so no visual change until edited). `eden_button` and `primary_dialog_button` now both branch on `ui.is_enabled()` (confirmed via egui's own source that this correctly reflects a caller's `ui.add_enabled(_ui)` wrapping) to pick a new `button_disabled_bg`/`button_disabled_text` pair instead of the normal one - previously there was no disabled/enabled distinction at all beyond egui's automatic 50% opacity fade, which still applies on top of whichever pair is chosen (per this session's confirmed decision). Both new fields default to exactly matching `button_background`/`button_text_color` (kept in sync via `regenerate_base_derived_colors`, same mechanism as `search_active_icon_bg`), so a disabled button looks pixel-identical to today until a user picks a genuinely different disabled color. 3 new picker rows added to the existing "Checkboxes & Buttons" section (which had room again after Phase 1 removed its redundant separator-handle row). 3 new i18n keys across all 6 locales. `cargo test` (95/95) and the i18n sync check both pass. **Live-verified**: scrolled the Appearance page to the Checkboxes & Buttons section and confirmed all 3 new rows render in the right place with the correct default colors - Primary Button Text (white), Disabled Button Background/Text both showing swatches identical to Button Background/Button Text just above them, exactly matching the "no visual change until edited" design. **Phase 5 (user-created custom themes) done.** New `CustomThemeEntry { id, name, accent, secondary }` + `CustomThemesSnapshot { next_id, items }`, persisted via a brand-new `custom_themes.bin` file (`core::indexer::{load_custom_themes, save_custom_themes}`) - deliberately **not** folded into `ThemeSettingsSnapshot`/`theme.bin`, so this new feature can never trigger the existing theme-reset fragility documented in `CLAUDE.md`. A saved custom theme is intentionally just a name + accent + secondary (the same shape as a built-in `ThemePresetDef`), not a full palette snapshot - applying one behaves exactly like clicking a prebuilt preset (sets `primary`/`secondary_accent`/`pinned_tab_color`/`button_favorite_fill`, then `regenerate_base_derived_colors`), matching the user's own description that "the attributes below will follow the saved custom theme value." New "Custom Themes" section placed **between** the existing "Color Scheme" and "Core Colors" sections in `customizetheme.rs`, as requested: a swatch grid (same two-color-preview shape as "Prebuilt Themes", click-to-apply, hover shows the name) each with a small trash icon, plus an inline name field + "Save Current Colors" button (gated on a non-empty trimmed name via `ui.add_enabled_ui`, mirroring `tags.rs`'s create-group pattern) that captures the *currently edited* palette's `primary`/`secondary_accent`. Deleting reuses `tags.rs`'s delete-confirmation-dialog pattern (`egui::Window` anchored center, "Theme name: ..." + OK/Close) rather than an instant delete, for consistency with how the app already handles deleting any other named, user-created item. New `ThemeCustomizerAction::CustomThemesChanged` variant persists the list on save/delete; applying a theme reuses the ordinary `ThemeUpdated` action since it's just a `primary`/`secondary_accent` edit like any other. 6 new i18n keys across all 6 locales. `cargo test` (95/95) and the i18n sync check both pass. **Live-verified end-to-end**: typed a name ("MyPurple"), clicked "Save Current Colors" and confirmed a new two-color swatch appeared with a selection outline and a trash icon; clicked the trash icon and confirmed the "Delete Custom Theme" dialog showed the exact theme name with OK/Close; clicked OK and confirmed the swatch was removed and the section returned to empty - the full save → apply → delete round-trip works. **Phase 6 (final pass) done.** Full `cargo test` (95/95), i18n sync check, and `cargo build --release` all pass clean. Confirmed `custom_themes.bin` is created correctly under `%LOCALAPPDATA%\ExplorerEden\` (2 bytes - an empty snapshot, matching the state after the Phase 5 live-verification's save-then-delete round-trip) - the new persistence file behaves exactly as designed and sits alongside every other per-feature file rather than inside `theme.bin`. Added two `CLAUDE.md` gotcha entries per this project's "update so the same mistake isn't repeated" rule: (1) a settings-page mode toggle that only updates its own `selected_*` state, with no path back to the actual live value it's meant to preview, looks exactly like a broken picker even though the underlying logic is fine; (2) a color field with a real, working picker but zero readers anywhere in the codebase is indistinguishable from a working one without grepping the *exact* field name across the whole codebase, not just confirming the section "looks complete." This closes out the full theme customization overhaul: every UI surface from the original request (sidebar, status bar, address bar, search, settings navigation, toolbar, preview pane, notifications, buttons) is now genuinely palette-driven and editable, a user-created custom-themes list exists between Color Scheme and Core Colors, and all three originally-reported Appearance-page bugs (Light mode not applying live, the dead Resize Handle field, missing bottom padding under the mode buttons) are fixed. **Follow-up (user found four real problems in the Custom Themes section right after Phase 5 shipped): no way to pick a secondary color when saving, the swatch list didn't align, saved themes showed no name, and clicking/saving in the list didn't work.** (1) **No secondary picker** - `secondary_accent` has no Core Colors row of its own (see its doc comment in `theme.rs` - it's normally only set as a side effect of clicking a whole prebuilt preset), so "Save Current Colors" was silently capturing whatever it happened to currently be with no way to actually choose it. Added a new `ThemeCustomizer.new_custom_theme_secondary` draft field with its own picker row directly above the name field, used for the save instead of reading `secondary_accent`. (2) **List misalignment and (4) broken select/save, same root cause**: each entry was a *nested* `ui.horizontal` (swatch + delete icon) inside the outer `ui.horizontal_wrapped` - a composite child `Ui` inside a wrapped layout throws off its own running-width bookkeeping (uneven gaps between entries) and, more seriously, interfered with hit-testing for widgets allocated inside that nested child, which is why clicking a swatch to apply/load it (and the surrounding list generally) didn't reliably register. Every other wrapped swatch row in this file (Prebuilt Themes, Color Scheme) is a single flat widget per entry for exactly this reason. Fixed by allocating each entry as one atomic rect (manually computed swatch/trash sub-rects, each with its own `ui.interact(..., Sense::click())` call using an explicit id keyed by the entry's own id) instead of a nested `ui.horizontal` - matching the pattern every other working swatch row already used. (3) **No visible name** - only ever shown as a hover tooltip. Added the name as real, always-visible text under each swatch (truncated to fit via the same `truncate_item_text` helper the sidebar uses for its own item labels), as part of the same atomic per-entry block. Also added save-as-update: saving under a name that already exists in the list (matched case-insensitively) now updates that entry's colors in place instead of creating a duplicate, with the button's own label switching between "Save Current Colors" and "Update Theme" depending on whether the typed name matches an existing entry. 2 new i18n keys (`theme_custom_theme_secondary`, `theme_custom_theme_update`) across all 6 locales. `cargo test` (95/95) and the i18n sync check both pass. **Live-verified**: created two custom themes and confirmed the swatches now sit evenly spaced with their names visibly printed underneath (previously only available via hover), clicking a swatch actually retinted the whole app to that theme's colors (previously unreliable), and the new Secondary swatch showed a genuinely independent, editable color. **Note**: while live-testing this, the custom-themes list changed between screenshots in a way not caused by any input this session sent - the user was independently testing the same live window at the same time (a known recurring situation for this app, already documented elsewhere in `CLAUDE.md`); automated interaction with the running window was stopped as soon as this was noticed, in favor of finishing verification via the code path and the earlier controlled test. **Follow-up (user asked: how to change a selected custom theme's secondary color and save it back to the same entry?).** There was no direct path - clicking a swatch to select/apply a theme didn't pre-fill the name field or the Secondary picker with that theme's own values, so re-saving under the same name required retyping it from scratch and separately remembering/re-picking its secondary color. Fixed by having the swatch click handler also copy `entry.name`/`entry.secondary` into `customizer.new_custom_theme_name`/`new_custom_theme_secondary` - selecting a theme now pre-fills both, so adjusting the Secondary swatch and clicking the button (already reading "Update Theme" once the name matches) saves the change back into that same entry. `cargo build`/`cargo test` (95/95) pass; no i18n changes. |

## Phase 8 — Networking & devices

| State | Item |
|---|---|
| Completed | SMB network browsing (`\\SERVER`, `\\SERVER\Share`, sidebar Network entry) |
| Completed | Shared Network auto-discovery via NetBIOS browsing |
| Completed | Portable device support (iPhone, Android, MTP) |
| Completed | Raw / unmounted drive detection (ISO sticks, Linux partitions) |
| Completed | **Additional network discovery protocols** — mDNS and WS-Discovery, two more ways for "Shared Network" to find devices that don't announce over legacy NetBIOS browsing (the sidebar's existing `enumerate_network_level`/`scan_local_subnet` discovery). Both are hand-rolled with **zero new dependencies** (matching this module's existing style - it already hand-rolls raw NBSTAT parsing and Win32 WNet browsing rather than reaching for a crate), sending one query and listening briefly for replies: `core/network_mdns.rs` sends a `PTR _smb._tcp.local.` mDNS query with the unicast-response (QU) bit set (so replies come straight back to our own ephemeral port - no need to bind port 5353 or join the multicast group ourselves, which could conflict with a real mDNS responder already on the machine), then chains PTR -> SRV -> A through a small hand-rolled DNS-message reader (with `0xC0` compression-pointer following, since NBSTAT parsing didn't need that) to resolve each responder's IPv4. `core/network_wsd.rs` sends one WS-Discovery `Probe` (a small SOAP/XML template, `quick-xml` - already a dependency, used today by the PDF/DOCX preview extractor - parses the `ProbeMatch` replies) to the WS-Discovery multicast address and reads whichever IP comes back in `wsd:XAddrs` (falling back to the reply packet's own source address). Both modules only return candidate IPv4s, not `NetworkComputer`s - `network.rs`'s `discover_network_computers()` runs every candidate through the *same* SMB-port-445 verification and NetBIOS name resolution the existing subnet sweep already uses (a new shared `has_smb_open` helper, extracted from `probe_smb_host`'s inline check), so a device that merely answers mDNS/WSD but doesn't actually serve SMB - a printer answering WS-Discovery, say - never shows up in "Shared Network" faking a browsable share. **Bundled fix:** `get_network_computers_cached()`'s background rescan never called `request_repaint()` after refreshing its cache (confirmed - unlike every other background-thread feature in this codebase, which all wake the UI this way), so a fully idle window could sit on a stale device list; added the missing call, which matters more now that the scan can take noticeably longer (two ~1.5-2s multicast round-trips). 5 new unit tests (hand-built valid mDNS response and WS-Discovery XML fixtures, plus malformed-input cases) verify both parsers in isolation. Live-verified: built clean in both debug and release with no `Cargo.toml`/`Cargo.lock` changes, ran on this machine's real LAN with temporary debug logging (removed before done) confirming both new discovery calls execute without error and return gracefully (empty, on this particular network - no mDNS/WSD responder present here to positively trigger the match path, but the existing NetBIOS/subnet-sweep hosts still appeared unregressed in the sidebar throughout). |

## Phase 9 — Persistence & settings

| State | Item |
|---|---|
| Completed | Per-concern binary settings files under `%LOCALAPPDATA%\ExplorerEden\` |
| Completed | Export / Import Settings as a single JSON (settings, favorites, tags, both palettes) |
| Completed | Folder-size scanning toggle |
| Completed | Drive caching (30s) and metadata-keyed icon caching with background loading |
| Completed | Settings pages — General, Behavior, Startup & Window, Appearance, Favorites, Custom Context Menu, Tab Groups, Tags, Advanced |

## Phase 10 — Internationalization

| State | Item |
|---|---|
| Completed | Fluent-based i18n with OS-locale detection |
| Completed | Locales — en-US, id-ID, ja-JP, zh-CN, zh-HK, zh-TW |
| Completed | All code-referenced keys verified present in all six locales |
| Completed | Dead keys removed (`tooltip_show_file_explorer`, `tooltip_show_tags`) |
| Pending | Wire `i18n-sync-check` into CI so untranslated keys fail the build |

## Phase 11 — Engineering infrastructure & documentation

| State | Item |
|---|---|
| Completed | `CLAUDE.md` — architecture map, conventions, safety rules, recorded mistakes, operational instructions |
| Completed | `PRD.md` — product requirements, gaps, planned and proposed enhancements |
| Completed | `CHECKLIST.md` — this file |
| Completed | `eden-explorer-ui-test` skill — build / launch / click / drag / keys / screenshot with focus and occlusion verification |
| Completed | `egui-patterns-reference` skill — four documented egui patterns |
| Completed | `egui-design-review` skill — UI/UX consistency checklist |
| Completed | `i18n-sync-check` skill — locale-parity checker script |
| Completed | Automated release pipeline (semantic-release + GitHub Actions) |
| In-Progress | **README feature list is behind the code** — missing Delete Permanently, preview copy, tag-table columns, padding standardization, and the This-PC sort fix |
| In-Progress | **README documentation drift** — claims a "built-in performance benchmarking system" in two places; no such code exists in `src/` |
| Pending | Change Log file — none exists in the repo yet; decide whether to add one or rely on semantic-release notes |
| Pending | Scripted UI regression suite built on the `eden-explorer-ui-test` skill |
| Pending | Local-only panic / crash logging with a "copy diagnostics" action |

## Phase 12 — Backlog (not scheduled)

Rationale and detail for each item is in `PRD.md` §9.

### Search
| State | Item |
|---|---|
| Completed | Recursive / whole-drive search — see Phase 1's global search entry (Everything or built-in engine, recursive "This folder" or "Everywhere" scope, `*`/`?` wildcards) |
| Completed | Content search inside text files — see the Content search entry above (`content:<word>`, folder-scoped, built-in engine streams a line scan; Everything engine via its own native keyword) |
| Completed | Filter syntax — `size:>100mb`, `ext:png`, `modified:today` — see the Global search entry above (`parse_search_filters` in `core/fs.rs` for the built-in engine; Everything's own query parser already accepted this natively) |
| Completed | Saved searches in the sidebar — see the Saved Searches entry above (dedicated sidebar section, capped at 50, persisted to `saved_searches.bin`) |
| Completed | **"Save Search" from a search-results tab's own right-click menu** — previously the only way to save a search was the bookmark icon next to the navbar's search box; a search-results *tab* (once opened) had no way to save itself if that box was no longer visible/active. Added a "Save Search" entry to the tab strip's own right-click menu (`tabs.rs`), shown only when the right-clicked tab is a search-view tab (`core::fs::parse_search_view_path` on the tab's path), reusing the exact `(query, SearchScope)` shape and `SavedSearchesState::add` handler the navbar's bookmark button already produces - both paths funnel into the same code in `mainwindow_imp.rs`. Disabled (showing the existing `save_search_limit_reached` message) once already at the 50-saved-search cap, mirroring the bookmark button's own limit handling; a new `saved_search_count: usize` parameter threads the count into `tabs.rs` for this check. **Verification note:** build is clean and all 81 unit tests pass, but live-click verification wasn't completed this session - the app kept losing synthetic-input focus back to the user's own active window before a screenshot could confirm the menu item, consistent with several other features this session hitting the same contention. The change itself mirrors an already-shipped, already-tested code path almost exactly (same action shape, same handler), so risk is low. |

### Workflow
| State | Item |
|---|---|
| Completed | **Bulk rename with pattern / regex preview** — selecting multiple items and choosing "Rename" opens a dialog (`gui::windows::containers::bulk_rename`) instead of the single-item click-to-rename gesture. Simple mode: literal find/replace (case-insensitive toggle) plus `{name}`/`{ext}`/`{n}`/`{n:03}` placeholders (`{name}` resolves to the *original* stem, not the post-replace text - deliberate, documented precedence); Regex mode: `regex` crate find/replace with `$1`/`$2` capture-group references, `(?i)` injected for the same case-insensitive toggle, invalid patterns shown inline without ever partially applying. Live preview recomputes only on pattern change (not every frame), with per-parent-directory collision detection (both within the batch and against untouched existing files, correctly excluding names about to be vacated by another item in the same batch) - "Rename All" is hard-blocked while any row collides, is invalid, or the batch is a no-op. Commits via one `IFileOperation` batching every `RenameItem` call before a single `PerformOperations()` (`rename_paths_native`, mirroring `delete_paths_native`'s shape), with a per-item fallback for anything that didn't land, one batched notification, and tag remapping per successfully-renamed item. Modal built on `egui::Area`/`Frame::popup` (not `Window`), following the paste-conflict modal's established pattern. F2 also opens Bulk Rename when more than one item is selected (previously it only ever renamed one, inconsistent with the right-click menu's own multi-select branch). A "Bulk Rename Guide" section in README.md documents both modes with worked examples, including the `Find: .*` idiom for replacing an entire original name in Regex mode (verified via a dedicated regression test that Rust's `regex` crate doesn't double-apply a pattern that can also match an empty string at the end of input). 14 unit tests cover the pattern engine and collision detection directly. Live-verified end-to-end via the F2 keyboard path (mouse clicks were unavailable this session): opened over a multi-selection, typed a find/replace pattern, watched the preview update live, committed via Tab+Space navigation, and confirmed the rename actually landed on disk with a completion notification. **Follow-up (visual polish):** the modal previously had a plain text title with no icon badge, unlike the Checksums/Paste Conflict modals it otherwise mirrors structurally - now uses the same shared `modal_frame`/`modal_icon_header` helpers (`core/utils/widgets.rs`) those two already use, gaining an accent-tinted icon badge, a subtitle showing the selected item count, and the same accent-tinted modal border. Not re-verified live this session (see the click-unresponsive note on the theming entry above); the change only substitutes already-proven, already-tested primitives for what was hand-written inline before. |
| Completed | **Compress (zip) context-menu entry** — a new "Compress (zip)" item between the custom-context-menu group and Cut, each with its own separator (`itemviewer_helper.rs`). Naming (`core::compress::compress_target_path`): a single selected file names the zip after its own stem (extension replaced, e.g. `report.pdf` → `report.zip`); a single selected folder keeps its whole name (`Photos` → `Photos.zip`); multiple selected items are named after their shared parent folder. An existing zip of the same name is never overwritten - auto-numbered via the same `next_available_name` helper the paste-conflict modal's "Rename" choice already uses. Runs on a background thread (`compress_paths_async`) so a large folder doesn't freeze the UI, following the same start-a-job/poll-each-frame/finish-the-notification shape already established for robocopy pastes (`poll_pending_compress`, mirroring `poll_pending_paste`) - just without a live byte-progress bar, since zip has no external process to poll. Folders recurse (empty folders preserved as explicit zip entries), symlinks/junctions are added as opaque entries rather than followed (avoids an infinite loop on a self-referential link). 5 unit tests cover the naming rules, the collision auto-numbering, and a full round-trip (write a real zip, read it back with the `zip` crate's own reader, confirm entry names/nesting). |
| Pending | Workspaces — tab groups extended to capture split state, view mode, and sort |
| Pending | Dual-pane synchronized scrolling and folder compare |
| Completed | **Recent locations in the sidebar** — a new "Recent" section (`gui::windows::containers::structs::RecentLocationsState`), most-recently-visited first, capped at 20 entries. Recorded in `load_view_with_fallback`'s single normal-real-folder branch (`mainwindow_imp.rs`) - the one choke point every navigation trigger (Back/Forward/Up/tab click/sidebar click/breadcrumb entry) already funnels through to actually load a directory, so no per-trigger instrumentation was needed. A path already pinned to Favorites is never recorded (skips the redundant clutter). Revisiting a path already in the list moves it to the front instead of duplicating it. Click to navigate (reuses the existing `nav_to` action - a recent location is just a real path, no new sentinel needed), right-click for Open in new tab / Remove from Recent. Persisted to its own `recent_locations.bin`, mirroring Saved Searches' persistence shape. 6 unit tests cover the MRU ordering, dedup-by-move-to-front, cap, removal, and (new) `clear()`; live-verified end-to-end (navigated through two scratch folders, confirmed the sidebar showed them in correct order with real folder icons, confirmed persistence survived an app restart). **Clear All added later**: right-clicking the "Recent" section header itself (not an individual item) now offers a "Clear All" entry (only shown when the list isn't empty), wired through a new `SidebarAction::clear_recent_locations` flag exactly like the existing per-item `remove_recent_location`. `sidebar.rs`'s `draw_section_header` had to start returning the header button's `egui::Response` alongside its existing toggled-`bool` (all 7 section headers' call sites updated) so the Recent section could attach a `Popup::context_menu` to it. **Verification note**: the underlying `RecentLocationsState::clear()` is unit-tested and the build is clean, and the "Clear All" menu item was confirmed to render correctly and in the right place via live screenshots - but clicking it specifically could not be confirmed this session: every synthetic click aimed at it instead landed on the sidebar row directly above (the Saved Searches list's own item), while the same click coordinates correctly opened the context menu via right-click every time. No data was lost in the process (the Recent list was screenshot-verified untouched throughout), but live-click this one before fully trusting it. |

### File operations
| State | Item |
|---|---|
| Pending | Operation queue with pause / resume and aggregate progress |
| Completed | **Undo stack for moves and renames beyond Recycle Bin restore** — a shared `undo_stack`/`redo_stack` (`VecDeque<UndoableOperation>`, capped at 50, in-memory only, never persisted) on `MainWindow`. **Phase 1 (Rename) done**: renaming a single item via the `RenameRequest` handler pushes an `UndoableOperation::Rename { old_path, new_path }`; Ctrl+Z (`undo()`)/Ctrl+Y or Ctrl+Shift+Z (`redo()`) reverse/reapply it via the same `rename_one_native` call the rename handler itself uses, guarded so they don't also fire while a rename/bulk-rename dialog is open. A fresh rename after an undo correctly clears the redo stack (verified: Ctrl+Y then does nothing). **Phase 2 (BulkRename) done**: `BulkRenameCommit`'s existing `succeeded: Vec<(PathBuf, PathBuf)>` is pushed as `UndoableOperation::BulkRename { pairs }`; undo/redo reverse/replay the whole batch in one `rename_paths_native` call each. Verified: bulk-renamed 3 scratch files, Ctrl+Z restored all 3 original names in one step, Ctrl+Y reapplied all 3 renames in one step. **Phase 3 (`PendingPaste` extension) done**: added `renames: HashMap<PathBuf, String>` (the same map already passed to build robocopy jobs, now kept instead of discarded) and `origin: PasteOrigin` (`UserAction`/`UndoOf`/`RedoOf`, threaded through `start_robocopy_paste`'s new parameter and all 3 existing call sites as `UserAction` for now) to `PendingPaste` - no behavior change yet, both fields unused until Phase 4. Regression-verified: copy-pasted a scratch file into a folder with a same-named file already there, resolved the collision via Rename, confirmed the original was untouched and the incoming copy landed as `file-001.txt`. **Phase 4 (Move) done**: `UndoableOperation::Move { pairs, side }` pushed from `poll_pending_paste`'s `UserAction` success branch (final path computed per-source from `pending.renames`, falling back to `target_dir.join(file_name)`). `undo()` groups pairs by *original* parent directory (one `start_robocopy_paste` job per distinct directory, since a single job has exactly one destination) tagged `PasteOrigin::UndoOf`; `redo()` is always a single job (one destination, the original move's target) tagged `RedoOf`. A new `UndoRedoGroup` (shared `Rc`, tracked in `PasteOrigin`) only pushes the op onto the opposite stack once every sibling job in the group has reached a terminal state and all succeeded - a partial failure across a multi-directory undo pushes nothing, leaving the per-job notification (already shown automatically) as the only signal. Verified: cut-paste between two scratch subfolders, Ctrl+Z moved the file back (confirmed on disk), Ctrl+Y moved it forward again (confirmed on disk); repeated with a same-named collision resolved via Rename (`movefile-001.txt`), Ctrl+Z correctly restored it under its *original* name in the source folder without touching the pre-existing same-named file at the destination. Drag-and-drop's `MoveItems` handler already routes through the identical `start_robocopy_paste`/`PendingPaste` pipeline (updated in Phase 3) so it's covered by the same code path, though not separately UI-tested this pass (synthetic drag input is a known-unreliable gesture to script in this environment - see CLAUDE.md). **Phase 5 (Copy) done**: `UndoableOperation::Copy { pairs, side }` pushed alongside Move from the same `poll_pending_paste` `UserAction` branch (`pending.is_cut` picks which variant). Undo is synchronous - `delete_paths_native(created_paths, allow_undo: true)`, same shape as Rename/BulkRename's undo, sending the created copies to the Recycle Bin so they stay further-recoverable. Redo re-copies the sources via one `start_robocopy_paste` call (`is_cut: false`) tagged `RedoOf`, mirroring Move's redo. Verified: copy-pasted a scratch file, Ctrl+Z deleted the copy (confirmed via the Recycle Bin's `OriginalDirectory` metadata that it landed there, not gone, and that the source was untouched), Ctrl+Y re-copied it. **Phase 6 (Replace made non-destructive + undoable) done**: choosing Replace in the paste-conflict modal now recycles the pre-existing destination item first (`delete_paths_native(.., allow_undo: true)`) instead of letting robocopy overwrite it directly, then looks up that item's fresh Recycle Bin pidl via a new `find_recycled_pidl` (matches by original directory + name + most-recent deleted time - the same enumeration `load_recycle_bin_view` already does). The pidl is threaded through a new `PendingPaste.replaced`/`UndoableOperation::Move`/`Copy.replaced` field (`HashMap<final_path, pidl>`). Undo moves/deletes the incoming item away as usual, then restores the recycled original via `restore_paths_native`; redo re-recycles whatever currently occupies that spot fresh (a *new* pidl, since the file there now isn't the same one) before placing the incoming item again, so a subsequent undo still has a valid pidl to restore. Verified end-to-end including the trickiest case: paste onto a same-named file, choose Replace (original safely recycled, confirmed via Recycle Bin metadata); Ctrl+Z restored the original's *exact original content* (not just a placeholder) and removed the incoming item; Ctrl+Y reapplied Replace; a *second* Ctrl+Z still correctly restored the original content, confirming the re-recycle-on-redo path produces a valid pidl for the next undo. Also root-caused and fixed a UI-testing tooling bug hit during this verification (see CLAUDE.md and the `eden-explorer-ui-test` skill: `focus.ps1` and a following click/keystroke call must run in the *same* PowerShell process, or the new process spawned for the second call steals focus back). **Phase 7 (cap/polish/final regression) done**: the rename-dialog-open guard verified live (F2 on a file, Ctrl+Z while the edit box is open does nothing, confirmed the box stayed untouched); a final regression pass interleaving Rename + Move in one scratch session - Ctrl+Z twice undid Move then Rename (correct reverse order), Ctrl+Y twice redid Rename then Move (correct forward order), final state matched the pre-undo state exactly. `i18n-sync-check` passes cleanly (this feature added zero new user-facing strings - keyboard-only per the original scope, no Edit menu or toolbar button). The 50-entry cap itself (`push_undo`'s `while len > MAX_UNDO_STACK { pop_front() }`) was verified by code review rather than a live 55-rename loop - the logic is a two-line, already-reviewed eviction check with no async/ordering subtlety to hide a bug in, and a 55-iteration live UI loop was judged disproportionate to the risk given everything else in this feature has been live-verified end-to-end. All 7 phases of the approved plan are now complete. **Post-ship fix**: the Phase 6 recycle-before-replace call (and Phase 4/5's undo/redo re-recycle calls) reused `delete_paths_native`'s `FOF_WANTNUKEWARNING` flag, which is correct for the real user-facing Delete action but meant an internal, silent housekeeping delete could pop Windows' own native "permanently delete?" confirmation mid-operation if the shell decided the item couldn't be silently recycled - reported live by the user (a Replace triggered a native "Delete File" dialog). Fixed by adding a `silent: bool` parameter to `delete_paths_native`: `true` uses `FOF_NOCONFIRMATION | FOF_SILENT` (no prompt ever, a failure is just an `Err` the caller already checks), used by Replace's recycle-before-place and all of Undo/Redo's internal recycle/delete calls; the real Delete/Delete Permanently context-menu entries kept `silent: false` unchanged. Live-verified: Replace no longer shows any confirmation and still correctly recycles the original (confirmed via Recycle Bin metadata) and undo/redo still restore/re-recycle correctly; real Delete still recycles/warns exactly as before (no regression). |
| Completed | Richer same-name conflict resolution UI — see Phase 4's paste/drag-and-drop conflict modal entry |
| Completed | **Checksum / hash panel** — a "Checksums" context-menu entry (single real file only, right before Properties) opens a themed modal (`egui::Area`+`Frame::popup`, matching the paste-conflict modal's proven pattern - not `egui::Window`) computing CRC32/MD5/SHA-1/SHA-256 on a background thread (`core::checksum::compute_checksums_async`, one streamed pass over the file feeding all four hashers at once, mirroring `core::compress`'s async-job/poll-each-frame shape). Each hash has a copy-to-clipboard button (`copy_text_to_clipboard`, already used by the Copy Path context-menu entry); a "Compare" field lets the user paste a downloaded file's published checksum and see a case-insensitive match indicator naming which algorithm matched (or "No match"), without needing to know which algorithm it is. New dependencies: `crc32fast`/`md-5`/`sha1`/`sha2` (all pure-Rust, no build scripts). 2 unit tests hash known content (including an empty file) against independently-verified reference values. Live-verified: right-click gating (absent for folders/multi-selection, present for a single file), all four hashes matched independently-computed reference values exactly, the copy button put the exact hash on the clipboard, the compare field's match/no-match/cleared states all worked correctly. **Also fixed a real, live-reproduced input-safety bug found during this verification** (see CLAUDE.md): typing in the modal's Compare field and pressing Ctrl+A+Delete to clear it was *also* reaching the item viewer underneath and deleting the real selected file - a pre-existing gap (`itemviewer.rs`'s `modal_input_blocked` never accounted for `pending_paste_conflict`/`pending_bulk_rename`, only `tags_state.picker`) affecting all three `egui::Area`-based modals, not just the new one. Fixed for all three at once via a shared `egui::Context` memory flag set once per frame from `MainWindow::ui`, re-verified live that the same Ctrl+A+Delete sequence in the Compare field no longer touches the file underneath. **Follow-up (visual polish):** extracted this modal's (and the Paste Conflict modal's) hand-written frame/icon-header code into shared `modal_frame`/`modal_icon_header` helpers (`core/utils/widgets.rs`) - both modals, plus Bulk Rename, now use them, guaranteeing the accent-tinted border/icon-badge/title styling actually stays consistent across all three instead of three independent copies slowly drifting apart. The hash results now sit in their own bordered card (matching the paste-conflict modal's file-list card) with the algorithm labels colored with the accent, and the Compare match/no-match result is now a colored pill with a check/x icon instead of a plain colored line of text. Not re-verified live this session (see the click-unresponsive note on the theming entry above); backed by code review (the new helper calls are a straight substitution for what rendered identically before) and a clean build/test pass. |

### Preview
| State | Item |
|---|---|
| Completed | **Syntax highlighting for code files** — via `syntect` (configured with its pure-Rust `fancy-regex` backend, not the default C/Oniguruma `onig` one, to avoid a C-toolchain dependency, matching this codebase's existing AVIF/`dav1d` avoidance policy). New `core/syntax_highlight.rs` exposes one `highlighted_layout_job(text, token, font_id, dark_mode)` entry point (never fails - an unrecognized `token` falls back to a plain uncolored job) used by both consumers: the plain-text preview's `draw_selectable_text_with_copy` now attaches a `.layouter` to its `TextEdit` (egui 0.35's exact signature takes `&dyn TextBuffer`, not `&str`, and runs at least once per frame per egui's own docs - the resulting `Arc<Galley>` is cached per-widget, keyed by text length + dark/light mode, to avoid re-lexing every frame), and the rendered-Markdown preview's fenced-code-block renderer now highlights using the fence's own language tag (`` ```rust ``) when syntect recognizes it. Both paths use `SyntaxSet::find_syntax_by_token` (syntect's own purpose-built lookup for "a short token from the user," documented by syntect itself as being for exactly this Markdown-code-block-highlighting use case) so one function serves both a file extension (`"rs"`) and a fence language name (`"rust"`) without needing a mapping table between the two. The `.layouter` addition is purely additive - it doesn't touch the pre-existing `TextEditState`-read-for-copy logic at all (confirmed: `.layouter` only affects glyph layout/color, never selection or buffer state), so the existing right-click-to-copy mechanism is unaffected. 5 new unit tests. Live-verified: a scratch `.rs` file, a `.md` file with a fenced ```rust block, and a `.json` file (JSON needed no extra wiring - it's a bundled syntect syntax, so it started working automatically once this shipped) all render with distinct colors for keywords/strings/numbers/comments in both the plain-text and Markdown preview paths. |
| Completed | **EPUB text extraction** — `.epub` was previously routed to the generic zip-listing `Archive` payload (it's a zip container, so this technically "worked" but only showed the internal file tree). New `load_epub_preview` (`core/preview.rs`) walks the actual EPUB structure - `META-INF/container.xml` points to an OPF manifest/spine (`find_opf_path`), the OPF's manifest maps ids to hrefs and its spine lists them in reading order (`spine_content_paths`), and each resolved XHTML content document is tag-stripped into plain text (`extract_xhtml_text`, inserting a newline at block-level boundaries - `</p>`, `<br/>`, headings, list items - so chapters read as real paragraphs, not one run-on line; `<script>`/`<style>` content is skipped). No new dependency - reuses the same `zip::ZipArchive` + `quick_xml::reader::Reader` combo `load_docx_text_preview` already uses, since an EPUB is a zip container too. Result flows into the existing `PreviewPayload::Text` (no new payload variant), so it automatically gets Phase 1's syntax-agnostic rendering and Phase 5's future find-in-preview for free. 4 new unit tests cover the pure-parsing helpers directly (OPF-path lookup, spine ordering, tag-stripping, script/style skipping). Live-verified against a hand-built real EPUB (two XHTML chapters + manifest/spine): both chapters' text extracted correctly, in order, with paragraph breaks intact, and rendered in the actual running app's preview pane instead of a raw zip listing. |
| Completed | **7z archive listing** — new `load_7z_preview` (`core/preview.rs`), registered for the `.7z` extension, via `sevenz-rust2` (a maintained fork of the unmaintained `sevenz-rust`; RAR was deliberately skipped instead, since the only practical crate for it wraps the non-free-licensed official UnRAR library, which doesn't fit this project's open-source positioning). 7z is a completely different container format from zip, so it needs its own crate rather than reusing the `zip` crate - but only listing is needed here (no extraction), and `sevenz_rust2::Archive::open` already hands back every entry's name/size/directory-ness directly (`archive.files: Vec<ArchiveEntry>`) with no reader/decompression step required. Builds the *exact same* `ArchiveEntry` shape (`name`/`is_dir`/`size`/`depth`) `load_archive_preview` already produces for zip, so the existing indented-by-depth rendering in `itemviewer_preview.rs` needed zero changes - only a new loader function. 1 new unit test builds a real `.7z` archive via the same crate's own writer API (`ArchiveWriter`) and verifies `load_7z_preview` lists both a top-level and a nested entry with the correct depth. Live-verified: a real 7-Zip-created archive (a top-level file plus a nested subfolder) previews with the correct entries, sizes, and indentation, matching the existing zip-listing style exactly. |
| Completed | **Audio playback + waveform** — new `core/audio.rs`, mirroring `core/video.rs`'s shape: `AudioPlayer` reuses the same Windows Media Foundation `IMFMediaEngine` bootstrap (`CoCreateInstance(CLSID_MFMediaEngineClassFactory)`, the `IMFMediaEngineNotify` lifecycle callback, `Play`/`Pause`/`SetCurrentTime`/`GetCurrentTime`/`GetDuration`) `VideoPlayer` already uses, minus the WIC/`TransferVideoFrame` frame-pull code since there's no video track to decode - confirmed beforehand that Media Foundation already plays an audio-only file's sound fine even through the video path (it just showed the "no video track" placeholder). `AudioPreviewService` mirrors `VideoPreviewService`'s per-tab current-path management. The waveform itself needs real PCM samples, which `IMFMediaEngine` doesn't expose - a separate one-shot `decode_waveform` pulls the first audio stream via `IMFSourceReader` (requesting 16-bit PCM output regardless of the source's native format, so the decode loop doesn't need to handle multiple bit depths), mono-mixes multi-channel frames, and reduces the result to a fixed 400-bucket min/max peak buffer on a background thread - capped at ~20 million samples (`MAX_WAVEFORM_SAMPLES`, roughly 7.5 minutes of mono 44.1kHz audio) so a very long recording can't make the one-shot decode run unbounded, with a `truncated` flag surfaced in the UI when the cap is hit (mirroring the Archive preview's own truncation convention). `itemviewer_preview.rs` gained an `is_audio_extension` branch (mp3/wav/flac/aac/m4a/wma/ogg/opus/aiff/alac) reusing the *exact* transport-row pattern already built for video (play/pause button, current-time label, seek slider, duration label, all driven by the same `is_paused()`/`progress()`/`toggle_play_pause()`/`seek()` method shape `VideoPreviewService` already exposes) with a hand-painted min/max waveform (`draw_waveform`, matching this codebase's existing hand-drawn-widget style rather than pulling in a charting dependency) standing in for the video frame image, plus a white playhead line at the current progress fraction. No new dependency - `Win32_Media_MediaFoundation` was already enabled for video. Live-verified: a scratch synthesized WAV (a swept-amplitude 440Hz tone) previewed with a correctly-shaped waveform, a playhead that matched the reported current time, and working time/duration labels and seek slider; video preview was unaffected (untouched code path, confirmed by inspection - `audio.rs` and `video.rs` share no state). **Post-ship fix (also applied to video)**: a live test caught the audio file playing all the way through and stopping at the end on its own, despite neither `AudioPlayer::open` nor `pump_events` ever calling `Play()` - root-caused to `IMFMediaEngine`'s own `AutoPlay` COM property defaulting to `TRUE` regardless of whether the caller ever invokes `Play()` itself (confirmed directly in the `windows` crate's generated bindings: `IMFMediaEngine::GetAutoPlay`/`SetAutoPlay`, alongside a matching `GetLoop`/`SetLoop` pair). Both `VideoPlayer::open` and `AudioPlayer::open` now call `engine.SetAutoPlay(false)` and `engine.SetLoop(false)` right after creating the engine (before `SetSource`), and the `ENDED` lifecycle event now just pauses instead of seeking back to `0.0` and calling `Play()` again - so previewing a video or audio file no longer starts playback or loops on its own; the user has to press the play button, and playback stops (rather than repeating) once it reaches the end. |
| In-Progress | **Find-in-preview** — a small find toolbar (a toggle button, then a query field + Next/Prev + match count once there's a query, with 6px of breathing room below it) above the Text and Markdown preview payloads only (nothing textual to search in Image/Animated/Archive/Video/Audio). New `FindInPreviewState` (`gui/windows/containers/structs.rs`, alongside `RenameState`/`GalleryState` - plain per-tab UI state, reset whenever the previewed file changes) runs a case-insensitive substring search over the payload's own source string (the plain text, or the raw Markdown before rendering) and tracks byte-range matches, exposing both `current_match_range()` (the active match's byte range, readable every frame so the highlight stays visible) and a one-shot `take_jump_target()` (fires exactly once per fresh search or Next/Prev, so scrolling doesn't fight the user's own manual scrolling every frame). The matched word is actually highlighted, not just scrolled to: Text payloads bake the highlight directly into the `TextEdit`'s `Galley` via egui's own `text_selection::visuals::paint_text_selection` (the same function `TextEdit` uses to paint a selected range) - called manually rather than relying on `TextEdit`'s built-in selection painting, since that's gated on the widget actually holding keyboard focus, which this read-only preview widget never does (confirmed directly in egui's source: `builder.rs`'s paint step checks `has_focus` before calling `paint_text_selection` itself). Markdown payloads highlight the matched run *within* its rendered `Label`: `draw_markdown` switched from `Parser::new_ext` to its `.into_offset_iter()` variant (yielding each event's own source byte range), and a new `append_with_highlight` helper splits a text run into up to three `LayoutJob` sections (before/matched/after) when the current match's byte range overlaps that event's source range, giving the matched section a highlighter-pen background color (`find_highlight_bg_color` - theme-aware, distinct from the app's own accent color so a match doesn't get lost among links/headings) while leaving the rest of the run's formatting untouched; falls back to one plain append if the text doesn't line up byte-for-byte with its source range (e.g. a decoded HTML entity) rather than risk slicing at a non-char boundary. Scrolling still resolves to *block* granularity for Markdown (the match itself is highlighted precisely, but the scroll target is the containing paragraph/heading/list-item/block-quote, tracked the same way as before) since a `Label`'s rendered text doesn't carry per-character screen-position information the way `TextEdit`'s single buffer does; a match strictly inside a fenced code block or table cell (rendered via a `Frame`/`Grid`, not through the tracked `Label` path) is neither highlighted nor scrolled to - both called out in the code as intentional simplifications. 5 unit tests cover `FindInPreviewState` directly (case-insensitive multi-match search, empty-query clears matches, Next/Prev wrap-around, the jump-fires-once semantics, and the reset-on-file-change behavior). **Verification note**: build is clean and all 78 unit tests pass, and every egui/pulldown-cmark API involved (`CCursor`/`CCursorRange`, `paint_text_selection`, `Galley::pos_from_cursor`, `TextFormat::background`, `OffsetIter`) was confirmed directly against the installed crate sources rather than assumed from memory - but this phase still hasn't gotten a live click-driven UI pass: across two separate sessions, repeatedly attempting to switch a scratch tab into Preview view either landed on a real, pre-existing tab instead of the intended toolbar button, or silently did nothing at all (control-clicks elsewhere in the same window - a column header sort - worked fine, isolating the problem to this one toggle specifically), matching this project's own documented "click.ps1 can go unresponsive," "user may be driving the same window concurrently" gotchas. Live-verify this phase properly (toggle the find bar, confirm the matched word is visibly highlighted and the view scrolls to it, for both a `.txt` and a `.md` file with a term appearing later in the document) the next time a session can drive the app's toolbar reliably. **Follow-up (visual polish):** the find bar itself (`draw_find_bar`) previously took no `palette` at all and rendered with plain, unthemed `egui::Button`s - the only interactive row in the whole app with zero theme awareness, so it looked identical (and wrong) in whichever theme/preset was active. Now takes `palette`, renders the toggle as a borderless icon button that turns accent-colored when active, and (once active) wraps the query field + match counter + prev/next controls in a themed, rounded input-style frame (`palette.input_field_bg`/`borders_default`) matching the app's other input fields instead of a bare row of default-styled widgets; "no matches" is now colored with the app's existing critical/error color instead of plain weak text. Purely a styling change - the underlying `FindInPreviewState` matching/highlight logic (the actual subject of this phase's remaining verification gap) is untouched. |

### Accessibility & input
| State | Item |
|---|---|
| Pending | AccessKit / screen-reader support (also unlocks better UI automation) |
| Pending | Command palette (Ctrl+Shift+P style) |
| Pending | User-rebindable keyboard shortcuts |

### Extensibility & measurement
| State | Item |
|---|---|
| Pending | Documented plugin / extension manifest (command + icon + match rules + optional preview handler) |
| Pending | Reproducible local benchmark script — cold start, 10k-entry listing, idle memory — recorded per release |
| Pending | Track locale coverage and untranslated-key count as a release gate |
